# SS2 원버튼 필살기 — 기술문서

『사무라이 스피리츠! 2』(NGPC, 1999) 의 필살기를 **버튼 하나로** 내주는 입력 보조 엔진.
브라우저 실행기 · 레트로아크 코어 · 안드로이드 앱 세 가지로 이식되어 있으며, **엔진 본체는 세 곳이 같은 파일**을 쓴다.

> 게임 자체의 입력 결함과 플레이어인 저의 손 결함에 대해
> 모던 모드를 만들어보고자 하였던 몸부림입니다.

---

## 1. 문제 — 손이 느린 게 아니었다

사쇼!2는 커맨드가 안 나가는 일이 잦다. 흔한 진단은 "입력 판정이 빡세다"지만, 실제로 더 자주 일어나는 건 **제대로 넣었는데 다른 기술이 나가는 것**이다.

롬을 뜯어보니 커맨드 판정 루틴(ROM `0x202E8B`)이 이렇게 동작한다.

```
입력 링버퍼를 최신 → 과거 방향으로 훑는다
커맨드 토큰과 맞지 않는 샘플은 그냥 건너뛴다
토큰을 다 맞추면 발동
```

즉 **후방 서브시퀀스 매칭**이다. 연속일 필요가 없다. 그래서:

| 커맨드 | 토큰 |
|---|---|
| 파동 (짧음) | `2` `3` `6` |
| 승룡 (김) | `6` `2` `3` |

승룡을 넣는 동안 링버퍼에는 `6 2 3` 이 들어가는데, 그 안에 `2 3` 이 이미 들어 있고 그 앞 어딘가에 이전 입력의 잔재 `2`가 남아 있으면 **파동이 먼저 성립한다.** 긴 커맨드가 짧은 커맨드에 잡아먹힌다.

이건 플레이어 실력 문제가 아니라 매처 구조의 문제다.

### 게임 자신의 해결책을 찾아냈다

링버퍼를 직접 지우는 건 위험하다(다른 루틴이 커서를 참조한다). 대신 게임 안에 **자체 리셋 경로**가 있었다.

```
SYSTEM_RAM 0x1DCD 에 1 을 쓰면
루틴 0x202E1F 가 128바이트 링 커서를 리셋하고
다음 샘플 앞에 FF 센티널을 놓는다
```

**게임이 원래 갖고 있는 동작이다.** 프레임을 추가로 소모하지 않는다.

| 조건 | 긴 커맨드 성공 (7종 × 6회) |
|---|---|
| 무보정 | **0 / 42** |
| `0x1DCD` 리셋 적용 | **41 / 42** |

---

## 2. 램 맵

CPU 주소 = `0x4000` + SYSTEM_RAM 오프셋.

| 용도 | 오프셋 | 비고 |
|---|---|---|
| ACT (현재 동작) | `0x0E3E` | 기술 판별에 사용 |
| P1 Y | `0x0E3A` | 지상 = 128 |
| P1 X / P2 X | `0x0E38` / `0x0E78` | |
| **FACING** | `0x0E54` | 1 = 왼쪽을 봄 → 커맨드 좌우 반전 |
| **CHARSTYLE** | `0x1B51` | `8 × (2 × 캐릭터index + 유파)` |
| CARD | `0x1B56` / `0x1B57` | 카드 보유 |
| P1 HP / P2 HP | `0x1A46` / `0x1C46` | 최대 128. 플레이어 구조체 간격 0x200 |
| P2 Y / P2 ACT / P2 FACING | `0x0E7A` / `0x0E7E` / `0x0E94` | P1 + 0x40 대칭 |
| **P2 CHARSTYLE** | `0x1D51` | P1(0x1B51) + 0x200 대칭. 매치업 해설에 사용 (2026-08-18 실측 확정) |
| **HITSTOP** | `0x1DC7` | 맞는 중 = 입력 무시 |
| **CMDRESET** | `0x1DCD` | 1을 쓰면 링 리셋 |
| 입력 링버퍼 | `0x1DD1` – `0x1E50` | 128바이트 |
| MODE | `0x00A7` | **240** = 메뉴·카드·라운드 연출·컷씬, **241** = 전투 활성. GO 직후 0~1프레임은 241이어도 커맨드를 안 받는다 (엔진은 4프레임 예열 후 발동) |
| **패드 레지스터** | `0x2F82` (CPU `0x6F82`) | 아래 비트 |

패드 비트

```
UP 0x01   DOWN 0x02   LEFT 0x04   RIGHT 0x08
A  0x10   B    0x20   OPTION 0x40
```

액션 ID (allcards 한글롬 실측): idle `0x8`, 걷기 `0x18~0x2C`, 런 `0x44`,
기본기 `0x50~0x9C`대, 피격·다운·기상 `0xE0~0x17F`, **필살기 ≥ `0x180`**.
⚠️ 롬 버전에 따라 액션 ID가 다르다 — v0.96 롬에서는 같은 약베기가 다른 값으로 보고됐다.
실측을 대조할 때는 롬 버전부터 맞출 것.

---

## 2.5 콤보 — 실측으로 결론이 났다

"기본기에서 필살기로 콤보가 안 이어진다"는 체감 제보가 있었다. 엔진 탓인지 게임 설계인지
프레임 단위로 실측했다 (2타 순간 상대가 피격 경직 중일 때만 콤보로 인정하는 엄밀 기준).

- 약베기 히트 경직 **36프레임**. 최속 캔슬 +22f에 필살기 시동을 더하면 임팩트가 ≈ **44f** — **8프레임 부족**. 진짜 콤보는 성립하지 않는다 (2타는 칩 데미지 2).
- 강베기 히트 경직은 **72프레임**이지만 넉백 때문에 미드스크린에서는 전부 헛친다.
- 공통 A연타 체인은 없다. "3연참"은 겐주로 삼련살처럼 **기술에 내장된** 연계다.
- 게임의 캔슬 창은 **22프레임 고정**으로 보인다 — SP를 5~15f 어디에 눌러도 발동은 22f.
- `0x1DCD` 리셋은 콤보와 무관하다 (리셋 유/무 전 구간 동일).

**결론: 기본기→필살기 콤보가 안 되는 건 게임 설계다.** 엔진은 게임이 허용하는
최속(+22f) 캔슬을 그대로 살리고 있고, 그 이상은 게임에 없다.
플레이판 v1.3에서 [콤보] 버튼을 폐기한 판단이 이 실측으로 정당화됐다.

---

## 3. 엔진 설계

### 3.1 하는 일

버튼이 눌린 프레임에 두 가지만 한다.

1. `0x1DCD` 에 1바이트 → 게임이 스스로 링을 정리한다
2. 현재 캐릭터·유파에 맞는 커맨드를 **방향에 맞춰 뒤집어** 프레임 단위 큐에 넣는다

이후 매 프레임 큐에서 한 스텝씩 꺼내 패드 레지스터에 쓴다.

**게임 데이터를 수정하지 않는다. 없는 기술을 만들지 않는다. 입력만 대신 넣는다.**

### 3.2 게이트 — 실기와 같은 규칙을 지킨다

| 상황 | 처리 |
|---|---|
| 히트스톱 중(`0x1DC7`) | 발동 안 함 |
| 공중 (`Y != 128`) | 공중기만 허용 |
| 지상기 예약 중 공중 진입 | 착지까지 대기 |
| 카드 필요 기술 (`flags & 2`) | 카드 없으면 목록에서 제외 |
| 잡기 (`flags & 16`) | **헛손질도 그대로 나간다** (실기와 동일) |
| 라운드 연출 (`MODE != 241`, 4프레임 예열 전) | 보류 (v13) |
| 피격·다운·잡힘 (`ACT 0x100~0x17F`)·필살기 중 (`≥ 0x180`) | 보류 (v13) |
| 자기 기본기 중 (`ACT 0x50~0xFF`) | **즉시 주입** — 게임의 킬캔슬 창(+22f)을 살린다 (v14) |

v13 게이트의 배경: 라운드 연출·피격 중에 커맨드를 쏘면 게임이 앞부분(236의 2·3)을 버리고
꼬리의 →+A만 받아 **전진 기본기가 오발**된다. 보류는 행동 가능해지는 프레임에 그대로
발동하므로 연타 시 선입력 버퍼처럼 동작한다 (연타 발동 5→18회 실측).
v14의 완화: v13이 자기 약베기(0x74 등)까지 바쁨으로 취급해 캔슬이 +22f→+46f로 늦어졌다.
기본기 중에는 즉시 주입으로 되돌려 캔슬을 복구했다. ⚠️ 판정을 `act < 0x50`으로 되돌리지 말 것.
플레이판도 v1.4에서 같은 게이트를 이식했다 (JS, 33ms 감시 + 25ms 선입력 폴링).

### 3.3 방향 자동 반전

`0x0E54` 를 매 발동 시 읽어 커맨드 토큰의 `4`↔`6`, `1`↔`3`, `7`↔`9` 를 뒤집는다.
**좌우 반전 스위치가 없는 이유가 이것이다** — 필요가 없다.

### 3.4 강/약 — 누르는 시간으로 가른다

버튼을 잡고 있는 동안 게임에 버튼 비트를 계속 넣는다.

```c
#define HOLD_FRAMES    6    /* 기본 유지 */
#define STRONG_FRAMES  9    /* 이 이상 = 강 */
#define MAX_HOLD      26    /* 안전 상한, 자동으로 뗀다 */
```

| 누르는 법 | 실제 버튼 유지 | 판정 |
|---|---|---|
| 톡 (1~3f) | 6f | 약 |
| 조금 (8f) | 8f | 약 |
| 지그시 (15f) | 15f | **강** |
| 계속 (30f) | 33f (상한) | 강 |

일반 공격(A·B)은 엔진이 손대지 않고 그대로 흘린다.

### 3.5 입력 속도 — 세 단계 모두 실용상 동등

긴 커맨드 5토큰, 각 20회, 지상·비피격 조건.

| 프로파일 | 성공률 | 발동까지 |
|---|---|---|
| 표준 | 20/20 · 20/20 | 433 ms |
| 빠름 | 19/20 · 18/20 | 350 ms |
| 최속 | 20/20 · 20/20 | 267 ms |

---

## 4. 기술표

`gen_moves.js` 가 롬에서 뽑아 `ss2sp_moves.h` 를 자동 생성한다.

```c
typedef struct { const char *name; const unsigned char *motion; unsigned char len;
                 unsigned char btn; unsigned char flags; } ss2_move;
typedef struct { const char *id; const ss2_move *mv; unsigned char n; } ss2_style;
/* flags: 1=근접 2=카드필요 4=공중 8=미검증 16=잡기 */
```

**코어·앱판 기준: 15캐릭터 / 30유파 / 171기술.**
브라우저판은 분노 MAX 기술까지 포함해 더 많다 (예: 아수라 수라 8 vs 7).

기술명은 한글로 들어 있다.

```c
{"부동격", mo_kazuki_s_0, 3, 16, 0},
{"치류엔", mo_kazuki_s_4, 5, 16, 10},
```

> `ss2sp_moves.h` 를 다시 생성했으면 `ss2sp.o` 를 손으로 지워야 한다.
> make 가 헤더 의존성을 추적하지 않는다.

---

## 5. 세 가지 이식

엔진 본체 `ss2sp.c` 는 **세 곳이 같은 파일**이다. 붙는 지점만 다르다.

| | 붙는 곳 | 램 접근 |
|---|---|---|
| **브라우저** (EmulatorJS 4.2.3) | `getFrameNum()` 에 물린 매크로 루프 | JS에서 HEAPU8 스캔 |
| **레트로아크 코어** (Beetle NeoPop 포크) | `update_input()` 끝 한 줄 | `extern uint8_t CPUExRAM[]` |
| **NGP.emu** (emu-ex-plus-alpha 포크) | `runFrame()` 진입부 | **포인터 주입** |

### 램 접근이 갈리는 이유

libretro 판에서 `CPUExRAM` 은 전역 C 심볼이라 그냥 `extern` 하면 된다.
NGP.emu 는 같은 배열이 **C++ 네임스페이스 `MDFN_IEN_NGP` 안**에 있어 C에서 이름이 안 맞는다. 그래서 포인터를 주입한다.

```c
#ifdef SS2SP_RAM_POINTER
static uint8_t *ss2_ram_ptr;
void ss2sp_set_ram(void *p) { ss2_ram_ptr = (uint8_t *)p; }
#define CPUExRAM ss2_ram_ptr
#else
extern uint8_t CPUExRAM[16384];
#endif
```

**본문 코드는 양쪽 모두 손대지 않는다.**

### NGP.emu 쪽 특이사항

NGP.emu 는 `Emulate()` 안의 `storeB(0x6F82, *chee)` 가 주석 처리돼 있고 입력 이벤트 때 직접 쓴다.
그래서 프레임 직전에 한 번 덮어쓰면 엔진이 만든 커맨드가 그대로 들어간다.

가상 키를 실기에 없는 코드로 추가했다.

```
1~7   = NGP 실기 패드 비트와 1:1 (bit(code-1)) — 건드리면 안 됨
8~15  = SP1~SP8  (패드로 안 감. 엔진 트리거 비트로만)
16    = A+B      (터치로 두 버튼 동시 입력이 불가능해서 하나로 묶음)
```

### 왜 8개인가 — 전용 버튼을 만들지 않는 이유

`ss2sp_frame()` 은 트리거 비트를 **9개까지** 읽고, 버튼 직결 배치에서 `slot = b` → `mv[b]` 로 간다.
한 유파의 최대 기술 수가 **8개** (하조 극·시키 극) 이므로 **SP1~SP8 이면 버튼만으로 전부 닿는다.**

그래서 비오의·카드기술·초필살기용 **전용 버튼을 따로 만들지 않는다.** 전부 기술 목록의 한 항목일 뿐이고,
어느 슬롯에 놓을지는 기술배치 메뉴에서 정한다. 버튼을 늘리는 쪽이 아니라 **배정을 여는 쪽**이 옳다.

화면 버튼 개수에는 상한이 없다 — `VController::gpElements` 와 그룹의 `keys` 가 모두 `std::vector` 이고,
EmuFramework 의 **`Add New Button Group`** 이 `AppMeta::inputDeviceDesc(0).components` 를 그대로 띄운다.
실제 제약은 화면 넓이뿐이다.

---

## 6. 검증

네이티브 하네스에서 프레임 단위로 재현 시험.

| 항목 | 결과 |
|---|---|
| 버튼 배치 — 중립 발동 · 뒤로 걷다 발동 | **12 / 12** |
| SP 배치 — 5방향 × 중립·뒤걷기 | **15 / 15** |
| 강/약 트리거 유지 | 1f→6f · 8f→8f · 15f→15f · 30f→33f |
| 커맨드 하이재킹 (7종 × 6회) | 0/42 → **41/42** |
| 3연참 연속 입력 손상 | 없음 (3/3) |

브라우저판은 별도로 회귀 스크립트(43개 검사)를 돌린다.

---

## 7. 성능 — 진짜 원인은 캔버스였다

브라우저판에서 **화면을 만지는 동안** 게임이 느려지는 문제가 오래 남아 있었다.

### 반증된 가설들

측정으로 하나씩 죽였다. 기록으로 남긴다.

| 가설 | 결과 |
|---|---|
| RGB565 변환 비용 | **틀림** — GL 후킹해 보니 이미 `RGB565` 로 업로드 중이었다 |
| `simulateInput` 4배 호출 | **틀림** — 벽시계의 0.001% (CPU ×4에서도 0.01%) |
| 내 DOM/CSS 핸들러 | **틀림** — 아래 대조실험이 뒤집었다 |

### 대조실험이 방향을 바꿨다

내 터치 핸들러가 붙은 십자키 대신, **핸들러가 하나도 없는 에뮬레이터 캔버스** 위를 문질렀다.

| | emu fps | 긴 프레임 |
|---|---|---|
| 대기 | 58.8 | 8 |
| 내 십자키 위 (핸들러 있음) | 51.4 | 70 |
| **맨 캔버스 위 (핸들러 없음)** | **43.5** | **123** |

핸들러가 **없는** 쪽이 더 느렸다. 원인은 JS가 아니라 **합성(compositing)** 이었다.

### 원인

EmulatorJS가 emscripten에 넘기는 캔버스가 `alpha: true`, `premultipliedAlpha: true` 로 만들어진다.
브라우저는 **매 합성마다 캔버스를 페이지와 섞어야 한다.** 손가락이 닿아 있으면 합성기가 이미 바쁘고, 그 여분이 에뮬레이터 루프를 굶긴다.

에뮬레이터 캔버스는 매 프레임 전 픽셀을 새로 쓰므로 **구조적으로 불투명하다.** 비칠 것이 없다.

### 조치와 효과

```js
proto.getContext = function (type, attrs) {
  if (type === "webgl" || type === "webgl2" || type === "experimental-webgl") {
    attrs = Object.assign({}, attrs || {}, { alpha:false, premultipliedAlpha:false });
  }
  return orig.call(this, type, attrs);
};
```

| 상황 | 전 | 후 |
|---|---|---|
| 대기 | 59.8 / 0 | 59.8 / 0 |
| 십자키 드래그 | 52.6 / 57 | **59.3 / 2** |
| 버튼 연타 | 59.2 / 5 | **59.9 / 0** |
| 매크로 연타 | 56.4 / 26 | **59.4 / 4** |
| 셰이더 `2xScaleHQ` | 49.1 / 86 | **59.5 / 1** |
| 셰이더 `4xScaleHQ` | 44.7 / 118 | 50.9 / 70 |

숫자는 브라우저가 그린 프레임이 아니라 **`getFrameNum()` 차이 = 에뮬레이터가 실제로 돌린 프레임**이다.
셰이더가 비싸 보이던 것도 상당 부분 이 합성 비용이 겹친 결과였다.

패치는 부팅 후에도 **되돌리지 않는다.** 셰이더 전환·전체화면에서 GL 컨텍스트가 재생성되면 그때도 불투명해야 한다.

> 상류 제보: [EmulatorJS/EmulatorJS#1255](https://github.com/EmulatorJS/EmulatorJS/issues/1255)

### 해서는 안 되는 것 (측정으로 반증됨)

- **vsync 끄기 금지**
- **오토 프레임스킵 금지**
- **입력 지연 늘리기 금지**
- **해상도 강제 하향 금지** — 59.1 → 44.7 fps 로 **역효과**

---

## 8. 빌드에서 걸렸던 것들

같은 실수를 반복하지 않기 위한 기록. 전부 한 줄짜리였다.

| 증상 | 원인 | 조치 |
|---|---|---|
| `Build base` 가 5분대에 사망<br>`CXX_MODULE_STD requires toolchain support` | `imagine/cmake/config.cmake` 가 `import std` 실험 UUID `451f2fe2…`(CMake 4.3)를 하드코딩. `get-cmake@latest` 가 4.4+ 설치 | 워크플로에서 `cmakeVersion: "~4.3.0"` 고정 |
| `Main.cc:116 storeB must be declared` | C++20 모듈에서 **인터페이스가 include 한 헤더는 구현 파일로 안 따라온다** | `Main.cc` 에 `#include <ngp/mem.h>` |
| 링크 시 `undefined symbol: CPUExRAM`, `ss2sp_set_ram` | `set_source_files_properties(... COMPILE_DEFINITIONS)` 가 먹지 않아 `SS2SP_RAM_POINTER` 가 컴파일 줄에 안 붙음 | `target_compile_definitions(ngpemu PRIVATE SS2SP_RAM_POINTER)` |
| 레트로아크에서 코어 옵션이 안 보임 | `libretro_core_options.h` 의 v1/v2 **양쪽 배열에** 선언해야 함 | 둘 다 추가 + `input` 카테고리 활성화 |
| 롬이 목록에 안 뜸 | 확장자 필터 | `.ngc .ngp .npc .ngpc` |
| 화면 버튼이 전부 같은 빈 원 | 그림판에 A·B·Option만 있고 나머지는 `blank` 로 떨어짐 | 그림판 256×128 → 256×256 확장, 7개 추가 |
| APK 이름 바꾸면 빌드 실패 | 워크플로가 `NgpEmu-release.apk` 를 하드코딩 | `metadata_pkgName` 은 유지, `metadata_id`/`metadata_name` 만 변경 |

### 스스로 반복한 실수

**초록 빌드 한 번 보기 전에 기능을 계속 얹었다.** 첫 컴파일이 실패할 걸 알면서도 그랬고, 덕분에 원인 분리가 늦어졌다. 다음부터는 **최소 형상으로 초록불부터** 본다.

---

## 9. 경계

- **롬은 어디에도 포함하지 않는다.** 저장소·페이지·아티팩트 어디에도 없다. 사용자가 자기 파일을 넣는다.
- 브라우저 실행기에서 롬은 브라우저 안에서만 열리며 전송되지 않는다.
- 레트로아크 코어는 Beetle NeoPop / Mednafen 기반 → **GPLv2**.
- NGP.emu 포크는 **GPL-3.0**. APK를 배포하려면 포크를 공개로 유지하면 조건이 충족된다.
- SAMURAI SPIRITS™ / SAMURAI SHODOWN™ 저작권은 SNK CORPORATION. **SNK와 무관한 비공식 팬 제작물**이다.
- 사쇼!2가 아닌 게임에는 엔진이 아무 일도 하지 않는다.

---

## 10. 저장소

| | |
|---|---|
| 브라우저 실행기 | https://rmdkdkr-png.github.io/ss2-sp-runner/ |
| 레트로아크 코어 | https://github.com/rmdkdkr-png/ss2-sp-core |
| NGP.emu 포크 | https://github.com/rmdkdkr-png/emu-ex-plus-alpha |
| 상류 제보 | https://github.com/EmulatorJS/EmulatorJS/issues/1255 |
