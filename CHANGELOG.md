# 변경 이력

## v0.5.6d — 뒤+A+B 비오의는 A+B 버튼에 불이 들어온다

제보: "뒤+A+B 하면 SP도 같이 클릭되는 효과가 보여서 거슬린다."

비오의를 A+B 로 내는데 **불빛은 SP 버튼에** 들어왔습니다. 발동 함수가 눌린 버튼을
`#spBig` 으로 못 박아 두고 있었습니다. 하단 알림도 「←+SP」로 떴고요.

- 어느 버튼에 불이 들어올지는 **부른 쪽이 정하도록** 바꿨습니다. 뒤+A+B 는 A+B 버튼에만 불이 들어옵니다.
- 하단 알림도 「←+A+B  비오의」로 바뀝니다.
- SP로 낸 기술은 전과 같이 SP 버튼에 들어옵니다.

회귀 108건 통과.

## v0.5.6c — 준비 화면마다 나오던 「천천히 골라라」 · 조사 (으)로

**한 판 끝나고 다음 준비할 때마다 메뉴 잡담이 나오던 것을 고쳤습니다.**

제보: "승리나 패배 후 캐릭 이름 나오고 다음 넘어갈 때 '천천히 골라라' 같은 말이 항상 나온다."

원인은 타이머였습니다. 메뉴 잡담은 *한 화면에 15초 이상 머무를 때* 나오게 돼 있는데,
그 타이머를 **화면이 바뀔 때만** 다시 셌습니다. 그런데 승패 이름 화면은 아직 전투 상태라
화면 전환 리셋이 안 걸리고, 그 뒤 조용히 전투만 빠져나오니 **전투 전에 세워 둔 낡은 타이머가
이미 만료돼 있었습니다.** 그래서 메뉴에 들어서는 순간 "15초 넘게 머물렀다"로 판정돼 매번 터졌습니다.

- **전투를 벗어나는 순간 타이머를 다시 셉니다.** 이제 실제로 그 화면에 오래 머물러야 나옵니다.
- **결과 뒤 10초 침묵(hush)을 메뉴 잡담·선택창 사담에도 적용**했습니다. 두 겹으로 막습니다.
- 코어판에도 같은 고침을 넣었습니다.

**조사 (으)로**

- 「츠바메 가에시**으로** 눕혔군!」 → 「가에시**로**」. 받침 판정을 넣었습니다.
  받침이 없거나 ㄹ 받침이면 「로」, 그 밖엔 「으로」. (「호월참으로」·「백귀살로」 모두 정상)

회귀 106건 통과.

## v0.5.6b — 귀신터치 2차 · 카드 캡처 안정화

**SP가 저절로 눌리는 것 — 라운드 중에도 났다는 제보를 반영해 다시 고쳤습니다.**

앞선 고침은 **판이 끝났을 때**만 막았습니다. 원인의 본체는 따로 있었습니다:
SP를 눌러도 지금 못 내면 **최대 1.5초까지 예약**해 뒀다가 낼 수 있게 되면 냈습니다.
그 사이 사용자는 이미 다른 걸 하고 있으니, 뒤늦게 나오는 기술은 전부 저절로 눌린 것처럼 보입니다.

두 겹으로 막았습니다.

- **예약 창을 1.5초 → 0.4초**로 줄였습니다(공중 재시도 1.2초 → 0.7초, 착지 대기 1.2초 → 0.67초).
  격투게임 선입력 관례(10~20프레임) 안쪽입니다.
- **그 사이 새 입력이 하나라도 들어오면 즉시 버립니다.** A·B·A+B·SP, 그리고 **새로 누른 방향**.
  방향을 뗀 것은 무시합니다 — 점프 중 방향을 놓아도 착지 기술은 살아야 하니까요.
- 코어판에도 같은 규칙을 넣었습니다.

**카드 아트 캡처가 제대로 안 되던 것**

화면이 **서는 도중에** 찍는 게 원인이었습니다. 페이드 중이거나 타일이 반만 올라온 프레임은
액자 판별을 통과해도 그림이 반쪽입니다.

- **280ms 간격으로 두 번 떠서 완전히 같을 때만** 담습니다(화면이 멈춰 있다는 뜻).
- 실패하면 0.5초 뒤 다시 노립니다 — 카드 화면에 머무는 동안 계속.
- 담으면 **"🎴 카드 아트 담았다 — n/4"** 로 알려 줍니다.

회귀 103건 통과.

## v0.5.6 — 홈 화면 설치(PWA) · 성능 측정 · 귀신터치와 뜬금 비오의 잡기

**고친 것 (사용자 제보 둘)**

- **판이 끝나면 뜬금없이 "온다! 비오의!"** — 승부가 난 뒤의 **승리 포즈도 액션ID가 0x180을 넘습니다.**
  그걸 필살기 발동으로 읽고 있었습니다. 이제 **둘 다 살아 있고 KO 처리 전일 때만** 기술로 봅니다.
  라운드가 새로 서면(양쪽 만피) KO 표식을 풀어, 다음 라운드 기술 알림이 막히지 않습니다.
- **SP가 귀신터치마냥 눌리던 것** — SP를 누르면 바로 못 낼 때 최대 1.5초까지 **예약**해 뒀다가
  낼 수 있게 되면 냅니다(선입력 게이트·공중 재시도·착지 대기). 라운드가 끝나는 순간 누른 입력이
  그대로 남아 **다음 라운드에서 뒤늦게 터졌습니다.** 이제 전투가 끝나면 예약을 통째로 버립니다.
  두 고침 모두 코어판에도 같이 넣었습니다.

**PWA — 홈 화면에 설치**

- 상단에 **[설치]** 버튼이 뜹니다(설치 가능한 브라우저에서만). 누르면 홈 화면에 들어갑니다.
- 설치하면 **브라우저 껍데기 없이 전체화면**으로 뜨고, 주소창이 먹던 세로 공간을 회수합니다.
- **오프라인으로 돕니다.** 에뮬 자산(약 5MB)을 서비스워커가 캐시합니다.
  롬은 캐시에 넣지 않습니다 — 롬은 예전처럼 IndexedDB 에만 있습니다.
- 새 판이 올라오면 알려 주고, 새로고침하면 바뀝니다.

**성능 측정 — 브라우저끼리 숫자로 비교**

- 설정 맨 아래 **[측정 시작]**. 16초 동안 두 구간을 잽니다 — ① 무입력 8초 ② 디패드 문지르기 8초.
- 재는 값: **에뮬 fps**(화면 표시가 아니라 게임이 실제로 돈 프레임), rAF 간격 중앙값/p95/최대,
  33ms 초과 끊김 횟수. 기기·브라우저·설정도 같이 찍힙니다.
- **결과 복사** 버튼이 있습니다. 삼성 브라우저와 크롬에서 각각 돌려 붙여 넣으면 바로 비교됩니다.

회귀 101건 통과.

## v0.5.5 — 카드 그림을 정확히 잘라 세운다 · 이름판은 게임 글꼴 그대로

카드 아트가 이 게임 최고의 캐릭터 아트라는 지적을 반영했습니다. 화면째 뜨던 것을
**액자만 정확히** 잘라내고, 글자는 우리가 그리지 않습니다 — 게임이 세로로 그려 둔 이름판을 그대로 떠 옵니다.

- **카드 그림 96×96 정위치 크롭.** 코어 프론트엔드로 실기 화면(scr 6 「능력 추가」)을 떠서
  픽셀 단위로 잰 좌표를 씁니다 — 그림 `x32,y16` 96×96, 이름판 `x8,y8` 16×88, 유파판 `x136,y8`.
- **이름판·유파판은 게임 글꼴 그대로.** 왼쪽 판에는 캐릭터 이름, 오른쪽 판에는 유파가
  그림 아래에 섭니다. 우리가 그린 글씨는 한 자도 없습니다.
- **아무 화면이나 뜨지 않습니다.** 액자 위아래 두 줄이 새까만지 확인해서, 카드 그림이
  실제로 떠 있을 때만 담습니다.
- **후보 4장까지 모읍니다.** 새 카드를 볼 때마다 쌓이고, 상단 🎴 버튼이
  `카드 아트 1 → 2 → … → 스테이지만 → 끔` 순으로 돕니다. 롬별로 저장됩니다.
- 캐릭터 선택창에서 뜨던 초상 캡처는 뺐습니다 — 카드 그림이 나오면 그쪽이 항상 낫습니다.
- 회귀 97건 통과.

## v0.5.4 — 양옆 아트웍을 스테이지 배경으로

지적 반영: 세로 캐릭터 이름은 이질적이고, 도트 초상을 크게 늘린 것도 좋지 않았습니다. 둘 다 뺐습니다.

- **양옆이 스테이지 배경으로 채워집니다.** 지금 화면을 그 자리에서 떠서 흐리게 깔고,
  왼쪽은 거울처럼 뒤집어 이어지게 했습니다. 화면 밖으로 스테이지가 번져 나가는 느낌입니다.
- **캐릭터마다 그림이 필요 없습니다.** 롬 주소도, 캐릭터별 표도 안 씁니다 —
  화면을 뜨는 방식이라 **다른 게임에 이식해도 그대로 돕니다.**
- **카드 그림 모드**: 카드 모음집 화면을 열면 그 그림을 떠 뒀다가 양옆에 액자로 세웁니다.
  상단 🎴 버튼으로 **스테이지 / 카드 그림 / 끔** 을 돌립니다.
- 세로 붓글씨 이름, 도트 초상 확대, 초상 잔무늬는 제거했습니다.
- 캡처는 판이 설 때와 카드 화면에서만(최소 3초 간격) — 프레임마다 도는 것은 없습니다.
- 회귀 97건 통과.

## v0.5.3 — 시작 화면을 옛 콘솔처럼 · SP로 시작 · 대전 사이 대사 정리

- **시작 화면이 옛 콘솔 부팅 화면이 됐습니다.** 붓글씨 제목 + 픽셀 글꼴 + 스캔라인,
  가운데 **SP 원판**과 깜빡이는 `PRESS SP TO START`. 길게 늘어놓던 소개 문구와 기능 카드는 뺐습니다.
- **SP(또는 스페이스·엔터)로 게임을 시작합니다.** 저장된 롬이 있으면 바로 실행되고, 없으면 파일 선택이 열립니다.
- **대전 사이 승부 난 뒤 대사를 정리했습니다** (제보: "어색하다").
  - 결과 멘트는 **매치가 진짜 끝났을 때만**(2선승) 나갑니다. 라운드 하나 이긴 것으로는 말하지 않습니다.
  - 결과는 **두 줄까지** — 승패 화면 반응 + 한마디 더. 오늘 전적은 **세 판에 한 번**만 덧붙습니다.
  - 결과 직후 10초, 그리고 KO 연출 중에는 **혼잣말·스토리 사담을 잠급니다.** 결과 줄 사이 간격도 넓혔습니다.
- **SP 자리 고르기: 가까운 자리 표(7×7)를 씁니다.** 잡은 방향의 자리가 비었거나 카드가 없으면
  대각→아래→앞뒤→중립 순으로 가까운 자리를 찾아 냅니다.
  **중립을 눌렀는데 중립 자리가 비어 있으면 다른 자리를 끌어오지 않고 그냥 베기(A)** 가 나갑니다.
- **양옆 아트웍에 전환 버튼**(상단 🎴)을 뒀습니다 — 자동 / 일러스트 / 초상 / 무늬만 / 끔.
  **캐릭터 선택·유파 선택 화면의 큰 일러스트**를 롬에서 잘라 캐시했다가 씁니다(그림은 배포물에 없습니다).
- 회귀 97건 통과.

## v0.5.2 — 화면 양옆 아트웍 · 설정 간소화 · 빈 자리엔 그냥 베기

- **화면 양옆이 검은 여백이 아니게 됐습니다.** NGPC 화면은 160×152라 가로가 남는데, 그 자리에
  **당신 롬에서 뽑은 초상 타일**로 액자를 세웠습니다 — 큰 초상 워터마크 + 잔무늬 + 세로 붓글씨 이름 +
  금색 테두리. 왼쪽은 내 캐릭터, 오른쪽은 상대. 그림은 배포물에 없습니다(로드된 롬에서 그 자리에서 그림).
  여백이 좁은 화면(56px 미만)에서는 자동으로 접힙니다. 매 프레임 도는 계산은 없습니다.
- **SP 자리도 중립도 비었으면 그냥 베기(A)가 나갑니다.** "SP를 눌렀는데 아무 일도 안 남"을 없앴습니다.
  자리 고르기 규칙 자체는 쓰던 그대로입니다 — 잡은 방향의 자리, 없으면 중립.
- **설정을 줄였습니다.** [발동 결과 표시]는 폐지(해설이 이미 말해 주는데 100ms 램 폴링만 남아 있었습니다),
  [메뉴 간소화]도 항상 켬으로 고정하고 항목을 뺐습니다.
  기본값 정리: 해설 켬 · 해설자 하오마루 · 기술명 표시 · 하단 알림 켬 · 게임 언어 자동 ·
  캐릭터 자동 켬 · 진단 표시 끔 · 화면 필터 없음.
- 회귀 97건 통과.

## v0.5.1 — 빈 자리·카드 없음 처리 · 연계 자동입력 폐지 · 입력 속도 최속 고정 · 비오의 호들갑

- **SP 자리가 비었을 때와 카드가 없을 때를 갈랐습니다.** 두 경우는 다른 상황입니다.
  - **빈 자리**(그 방향에 아무것도 안 둠) → **중립 자리 기술**로 폴백합니다. 중립도 비어 있으면
    아무 입력도 내지 않고, 왜 안 나갔는지 한 줄 알려 줍니다("이 자리엔 기술이 없다").
  - **카드 기술인데 카드 미장착** → **발동하지 않습니다.** 예전 코어는 그대로 커맨드를 넣어서
    엉뚱한 약/강베기(헛손질)가 나갔습니다. 이제 "카드가 없다 — 그 기술은 못 낸다" 로 이유를 알려 줍니다.
    (브라우저 실행기는 원래 막고 있었습니다 — 코어·앱을 여기에 맞췄습니다.)
- **연계 자동입력을 없앴습니다.** 갈포드 플라즈마 블레이드처럼 뒤에 A가 더 들어가면 기술이 깨져
  버린다는 제보가 계속 있었습니다. 연계는 손으로 넣는 게 맞습니다. 설정 항목도 삭제했습니다.
- **입력 속도는 최속 고정.** 표준·빠름은 발동만 느려서 고를 이유가 없었습니다. 설정 항목 삭제.
- **비오의는 나가는 순간 바로 호들갑**을 떱니다. 전에는 결합창(0.45초)을 기다리다가
  곧바로 맞으면 적중 멘트에 먹혀 예고가 사라졌습니다.
- 회귀 97건 (연계 폐지·속도 고정 검사 신설).

## v0.5 — 버튼을 넷으로: A · B · A+B · SP (비오의 = 뒤+A+B)

버튼이 너무 많았습니다. **A · B · A+B · SP 넷**으로 끝냅니다. 실행기·코어·앱 셋 다 같습니다.

- **비오의를 뒤 + A+B 로** 옮겼습니다. 유파를 가리지 않습니다.
  뒤를 잡지 않은 A+B 는 게임 원래 입력 그대로라 기존 조작을 뺏지 않습니다.
  (비오의는 분노 게이지가 차 있어야 나가는 원래 규칙 그대로입니다.)
- **SP2~SP8 버튼을 없앴습니다.** 기술은 전부 **SP + 방향**(기술 배치)으로 나갑니다.
  옛 설정이 기기에 남아 있어도 눌러봐야 아무 일도 없습니다 — 엔진이 무시합니다.
- 코어 옵션 **SS2 One-button Layout 은 삭제**했습니다(이제 배치가 하나뿐입니다).
  코어 패드는 **X 또는 R = SP**, **Y 또는 L = A+B** 입니다.
- 앱(NGP.emu)도 키 목록이 SP 하나와 A+B 로 줄었습니다. 기본 매핑은 패드 B = SP, 패드 Y = A+B.
- 회귀 96건 (뒤+A+B = 비오의 검사 신설, 실제 롬으로 확인).

## v0.4.13 — 코어 해설을 브라우저판과 같은 수준으로 (실행기 자체는 v0.4.8과 같습니다)

"되는 걸 왜 안 했냐"는 지적을 받고, 코어에서 빠져 있던 대사를 전부 옮겼습니다.

- **이벤트 25종 → 52종, 대사 155줄 → 400여 줄.** 브라우저판 대사표(SPEAKERS·CHAR_LORE)를
  손으로 옮기지 않고 **기계로 추출**해 C 표로 만들었습니다(오탈자 0, 원본이 바뀌면 다시 뽑습니다).
- 새로 들어온 것: **관계 대사**(화자와 상대 캐릭터의 인연) · **캐릭터 설정 한 줄** ·
  **기술 이름을 부르는 적중/다운/KO 반응** · 선제타 · 큰 한 방 · 피격 · 상대 필살기 경고 ·
  다운/피다운 · 벼랑끝 · 장기전 · 라운드 선취·열세·매치포인트 · 승패 화면 반응 ·
  **오늘 전적** · **설욕** · 세션 연승 · 캐릭터선택 사담 · 메뉴 죽치기 · 문구(VS) 화면 · 스토리 사담.
- 기술 이름은 **SP 버튼으로 낸 기술**만 부릅니다 — 그 표는 코어가 들고 있어 롬 버전과 무관합니다.
- **발화 대기열** 추가 — 한 순간에 두세 마디가 겹치면 1.6초 간격으로 순서대로 나옵니다
  (매치업 → 관계 대사처럼).
- **글꼴을 갈무리11 좁은꼴로** 교체했습니다. 높이 11px 그대로에 폭이 8px라 한 줄에 스무 자 —
  전에는 열두 자에서 잘려 두 줄로 접히던 대사가 이제 한 줄에 끝납니다.
- **글리프를 TTF 렌더 대신 BDF(비트맵 원본)에서 직접** 떴습니다. 안티에일리어싱 때문에
  가는 획이 날아가 "자"가 "지"로 보이던 것을 잡았습니다.
- 실측 페이스: 11분 플레이에 201줄 — 3.3초에 한 마디로 브라우저판과 같습니다.
  부하는 켬/끔 차이 프레임당 0.02ms.

## v0.4.12 — 해설을 화면 밖 띠로, 위·아래 선택 (실행기 자체는 v0.4.8과 같습니다)

- **화면 밖 띠가 기본값** — 게임 화면 밖에 30픽셀 띠를 붙이고 거기에 그립니다.
  게임 화면을 한 픽셀도 가리지 않습니다. **위·아래 중에 고를 수 있습니다.**
  위에 붙일 때는 게임 그림을 띠 높이만큼 아래로 밀어 그립니다.
- **띠에도 큰 글씨(갈무리11)** — v0.4.11에서 올린 글씨 크기를 띠에서도 그대로 씁니다.
  띠 높이를 20 → 30픽셀로 키워 두 줄이 들어갑니다. 얼굴·표정·타자 연출·금색 강조도 그대로.
- **아주 긴 대사는 작은 글씨로 자동 강등** — 큰 글씨 두 줄에 안 들어가는 대사만
  8px 두 줄로 접습니다. 잘리는 일이 없습니다.
- 화면 크기·비율을 원래대로 두고 싶으면 `Inside the screen` (화면 안 말풍선)을 고르십시오.
  이것도 위·아래가 있습니다.

## v0.4.11 — 코어 해설을 화면 안으로, 글씨는 크게 (실행기 자체는 v0.4.8과 같습니다)

"가독성이 떨어진다 · 디스플레이 안에만 출력 가능하냐"는 지적을 받아 표시를 갈아엎었습니다.

- **화면 안 말풍선이 기본값** — 화면 크기·비율을 **원래대로(160×152) 두고** 게임 화면 안에
  상자를 그립니다. 덧띠(152 → 172)로 화면비가 바뀌던 것이 싫으면 이쪽이 맞습니다.
  위·아래 중에 고를 수 있고, 덧띠 방식도 옵션으로 남겨 뒀습니다.
- **글씨 크기 8px → 11px** — 코어에 **갈무리11** 글리프를 새로 얹었습니다(대사에 쓰이는 323자).
  같은 화면에서 글자가 37% 커져 확실히 읽힙니다. 글자마다 그림자를 한 겹 깔아
  게임 화면 위에서도 뜹니다.
- **상자가 대사 길이에 맞춰 늘어납니다** — 한 줄이면 낮게, 긴 대사는 두세 줄로.
  화면을 가리는 시간은 대사가 떠 있는 2.5초뿐입니다.
- 얼굴·표정·타자 연출·금색 강조는 그대로입니다.
- 덧띠(`Below screen`) 모드는 높이가 20px뿐이라 작은 글씨(8px) 두 줄까지입니다.

## v0.4.10 — 코어 해설에 얼굴·글자·연출 (실행기 자체는 v0.4.8과 같습니다)

코어판 해설 띠가 글자만 있던 것에서 브라우저 실행기와 같은 결로 올라왔습니다.

- **얼굴** — 화자 초상이 띠 왼쪽에 16×16으로 나옵니다. 그림은 배포물에 넣지 않고,
  **사용자 롬에서 코어가 그 자리에서 그립니다**(브라우저판과 같은 롬 오프셋 표, 숫자만 임베드).
  64바이트 검사합이 안 맞는 다른 버전 롬이면 얼굴만 조용히 생략되고 글자는 그대로 나옵니다.
- **표정** — 이벤트별 감정 표를 붙였습니다. 이겼을 때는 얼굴이 밝아지며 까딱이고,
  몰렸을 때는 어두워지며 떨립니다. 등장할 때 한 번 튑니다.
- **글자** — 한 글자씩 찍혀 나옵니다(프레임당 두 자). KO·완승·역전·속공·연승·엔딩 같은
  한 방짜리 대사는 **굵은 금색**으로 나오고 띠가 붉게 번쩍입니다. 위쪽 경계선도 함께 튑니다.
- 실기 확인: 하오마루 초상·타자 연출·금색 강조·걱정 표정까지 스크린샷으로 확인했습니다.
- APK는 여전히 앱 알림(토스트)이라 글자만 나옵니다 — 얼굴·연출은 코어판·브라우저판입니다.

## v0.4.9 — 해설이 코어판·APK판에도 (실행기 자체는 v0.4.8과 같습니다)

브라우저 실행기에만 있던 **캐릭터 해설**을 레트로아크 코어와 안드로이드 앱(APK)으로 옮겼습니다.
셋이 **같은 엔진 소스**를 씁니다 — 실행기의 `commStep()` 을 C로 내린 `ss2comm.c` 입니다.

- **코어(libretro)** — 코어 옵션 3개 신설:
  `SS2 Character Commentary` (켬·끔) · `SS2 Commentary Speaker` (하오마루·나코루루·한조·갈포드) ·
  `SS2 Commentary Display` (화면 아래 띠 / 상단 겹침 / 하단 겹침 / 프론트엔드 알림).
  기본값은 **화면 아래 20픽셀 띠에 코어가 직접 한글을 그리는** 방식입니다.
  글꼴을 코어가 들고 있어(대사에 쓰이는 글자만 뽑은 갈무리 8×8 글리프 323자) 어느 프론트엔드에서도
  깨지지 않고, 게임 화면을 한 픽셀도 가리지 않습니다. 대신 화면 세로가 152 → 172로 늘어납니다.
  원래 화면비를 지키고 싶으면 상단·하단 겹침을 고르십시오.
- **APK(NGP.emu)** — 시스템 옵션에 `SS2 Character Commentary` · `SS2 Commentator`.
  안드로이드 시스템 글꼴을 쓰는 앱 알림으로 한 줄씩 나옵니다(화면은 건드리지 않습니다).
- **실기 검증** — 새 코어에 롬을 물려 무한대전을 돌렸습니다. 경기 시작(매치업) · KO · 패배 ·
  반피 경고 · 마무리 재촉 · 승패 후 한마디 · 캐릭터선택/유파선택/카드 화면 잡담 · 혼잣말까지
  실제로 떴고, 화면 아래 띠에 한글이 그대로 찍히는 것을 스크린샷으로 확인했습니다.
  긴 대사는 두 줄로 접힙니다.
- **부하** — 해설 켬/끔 실측 차이 프레임당 0.011ms (1030fps → 1043fps, 약 1%).
  램 읽기가 코어 안에서 일어나 브라우저판보다 오히려 쌉니다.
- 기술명은 롬 버전을 타므로 코어·APK 해설에서는 다루지 않습니다 — 흥·분위기 쪽입니다
  (v0.4 컨셉 그대로).

## v0.4.8 — 입력 개선: 짧은 탭이 씹히지 않습니다

- **최소 홀드(2프레임) 보장** — 코어는 프레임당 한 번만 입력을 읽습니다. 그래서 아주 빠르게
  누르고 떼면(16ms 안) 게임이 그 눌림을 **한 번도 못 보고 통째로 씹혔습니다.**
  실측: 4ms 탭이 **8번 중 5번 무시** → 최소 홀드 적용 후 **0번**. A·B·옵션 버튼 전부 적용.
  강 판정 임계(8프레임)와는 무관해서 약공격 성질은 그대로입니다.
- 회귀 95건 (4ms 탭 인식 검사 신설).

## v0.4.7 — 샤를로트 나찰 자동인식 · 입력 지연 실측

- **샤를로트 나찰이 자동인식되게** (제보) — 쿠로코 수라와 샤를로트 나찰이 게임 내부에서 같은
  블록 값(0x98)을 씁니다. 종전엔 이 경우를 쿠로코로 해석해서 샤를로트 나찰이 안 잡혔습니다.
  이제 **기본 해석을 샤를로트 나찰로** 두고, 쿠로코를 쓰는 중일 때만 쿠로코를 유지합니다.
  판정 로직을 순수 함수(blkToChar)로 분리해 회귀로 고정.
- **입력 지연 실측** — 디패드 딜레이의 정체를 숫자로 갈랐습니다(브라우저 계측):
  JS에서 방향을 넣으면 게임이 받는 데 **16ms(정확히 1프레임)**, 실제 터치는 이벤트가 우리
  코드에 도착하는 시점이 이미 40ms대이고 거기서 게임까지 다시 ~27ms. 즉 **우리 몫은 1~1.5프레임**
  으로 바닥에 붙어 있고, 나머지는 브라우저 터치 파이프라인과 에뮬 프레임 경계 — 구조적인 값입니다.
- 회귀 94건.

## v0.4.6 — 혼잣말 간격 단축 · 기술명 알림 완전 침묵

- **혼잣말 간격 3초** — 15초는 너무 뜸하다는 피드백. 이제 마지막 말로부터 3초(전투 중 6초)만
  조용하면 화자가 한마디 던집니다. 전투 중에는 공방 사이 정적이 잦아 조금 길게 잡았습니다.
- **[기술명] 숨김 = 하단 알림도 완전 침묵** — 숨김으로 두면 "SP 오보로가타나" 같은 하단 팝업이
  아예 뜨지 않습니다(이름만 빠지고 팝업은 남던 것 → 제보). [하단 알림] 끔은 그대로 마스터 스위치.
  두 설정 중 하나만 꺼도 즉시 화면의 팝업까지 지웁니다.
- 회귀 93건.

## v0.4.5 — 쉴 때는 화자답게: 혼잣말

- **침묵 대신 캐릭터 대사** — 해설창이 비고 7초가 지나면, 화자가 자기다운 혼잣말을 던집니다.
  상황(전투/메뉴·스토리/대사 화면)에 따라 결이 다릅니다:
  하오마루 "…술이 떨어졌군" · "…검이 근질거리는군" / 나코루루 "…바람이 조용해요" ·
  "리무루루는 잘 있을까요" / 한조 "……" · "…그림자는 서두르지 않는다" /
  갈포드 "파피, 앉아! …좋아, 착하지" · "심심한데 한판 더 어때!"
- 쿨다운 15초라 수다스러워지지 않고, 해설을 끄면 당연히 나오지 않습니다.
- 회귀 92건.

## v0.4.4 — 승패 후 한마디 더 · 하단 알림 끄기 · 연계 자동입력 끄기

제보 3건 반영. 회귀 90건.

- **승리(패배) 후 한마디 더** — 결과 화면에서 반응 → **소감 한 줄**("좋은 검이었다. 다음 상대가
  기다린다" / "…이겼다고 멈추지 마라" / "나이스 파이트! 파피도 꼬리 흔든다!") → 오늘 전적까지 이어진다.
- **[하단 알림] 설정 신설** — 화면 아래 뜨는 기술명 토스트를 **완전히 끌 수 있게** 했습니다.
  기존 [기술명] 설정은 해설 문장 전용이라 토스트가 남아 있던 것(제보). 이제 끄면 발동·불발·강약
  토스트가 전부 침묵합니다.
- **[연계 자동입력] 설정 신설** — 연계기(플라즈마 블레이드 등)에 A를 한두 번 더 넣는 동작을 끕니다.
  이 추가 입력이 다른 기술을 깨는 경우가 있다는 제보(갈포드 나찰) — 끄면 커맨드만 넣고 끝냅니다.
- 진단 HUD에 **터치 직후 지연** 항목 추가(터치 300ms 내 최대 프레임 간격·누적 끊김) —
  "터치할 때마다 느림"을 기기에서 숫자로 가를 수 있게. 데스크톱 실측은 유휴와 동일(끊김 0).

## v0.4.3 — 스토리 완주 채집 2차: 엔딩 반응

승리봇이 **유가(보스)까지 꺾고 엔딩·스태프롤까지 완주** 채집. 회귀 87건.

- **엔딩 반응** — 엔딩/스태프롤이 **또 다른 mode 값(199)**이라는 걸 발견해, 그 순간 화자가
  완주를 축하한다: "끝까지 베고 올라갔군!! 오늘은 내가 술을 사마!!" / "…완주. 수행의 끝을
  보았군" / "클리어다!! 오늘의 히어로는 너다!!" (세션 1회급, 쿨다운 90초)
- **연전 콜 안전장치** — 보스 스테이지에서 카운터가 안 오르는 정황이 있어, 스테이지 값이
  **실제로 올라갔을 때만** 말하도록 단조 증가 가드. 같은 판을 다시 봐도 중복·오보 없음.
  코스를 새로 시작하면(값 감소) 자동 리셋.

## v0.4.2 — 스토리 완주봇 채집: 연전 콜 · 캐릭터 대사 화면 반응

승리봇 3호가 1인 대전(스토리)을 8연전 완주하며 채집. 회귀 85건.

- **스토리 연전 콜** — 스테이지 카운터 RAM(0x17FE)을 새로 리버싱(무한대전에선 0 유지로 성격 확정).
  다음 상대로 넘어갈 때 "3연전째다. 계속 가자!" · 5연전 "손이 완전히 풀렸겠군" ·
  8연전 "여기까지 왔군… 이제 진짜배기들이다" — 진행감이 붙는다.
- **캐릭터 대사 화면 반응** — 초상+말풍선이 뜨는 화면이 **별도 mode 값(197)**이라는 걸 채집으로
  발견. 그 순간 화자가 한 마디: "무슨 말을 하는 거냐, 저 녀석" / "…말은 검으로 하는 것이다" /
  "명대사 타임이다!" (+장면 비트).

## v0.4.1 — 무한대전(서바이벌) 해설: 연승 콜·결산

승리봇이 무한대전을 11연승 주행하며 **연승 카운터 RAM(0x180B)을 리버싱** — 값==연승수,
1인대전·메뉴에선 0 (교차 검증). 이걸 게임의 진짜 카운터로 읽어서:

- **연승 콜**: 새 상대 진입마다 "5연승! 검이 뜨거워졌다!" — 3/7/10연승 단계별 톤,
  세션 신기록이면 "신기록이다!!" (붓글씨급 아님, 흥 톤)
- **결산**: 서바이벌에서 쓰러지면 "N명 베고 여기까지… 훌륭한 기록이다!" —
  게임이 보상 카드(연승 무쌍)를 주는 그 흐름에 맞춰서.
- 채집 부산물: 서바이벌 패배→보상 카드→선택 복귀 흐름 스크린샷 일습(작업 자료).
- 회귀 83건 (연승 콜·결산 신설).

## v0.4 — 페이스 재조정: 경기 중은 이전 속도로, 흥·썰 위주

"경기 중 대사량은 이전이 나았다. 정보전달보다 흥·썰·분위기" — 반영. 회귀 81건.

- **경기 중 v0.3 페이스 복귀**: 전황·반피·연타 라인 제거(정보성), 교착 잡담 9→12초.
  경기 중 발화는 결과·위기·역전·KO·완승·역전승 계열만.
- **시작 시퀀스 3절로**: 매치업 콜 → 관계/썰 → 글귀 반응 (상대 분석 정보 라인 제거).
- **경기 밖도 같은 페이스**: 선택창 사담 7초×3마디, 스토리 사담 쿨다운 15초로 복귀.
  "전환마다 반드시 반응"(확률 게이트 없음 + 장면 비트)은 유지 — 빈도가 아니라 반응성이므로.
- 오늘 전적 코멘트("오늘 3전 2승")·두 줄 디스플레이·붓글씨·갈무리는 그대로.

## v0.3.3 — 대사 두 줄 디스플레이

- 해설창을 두 줄 조판으로(높이 64px 고정 유지 — 게임 화면 안 밀림). 짧은 대사는 한 줄 크게(최대 26px),
  긴 대사는 **두 줄로 꽉 차게**(줄높이 한도 21px) — 대사가 길어져도 잘리지 않는다.
  대사량 확장 대비: 현재 대사 18KB/336줄, ×100(1.8MB)이어도 로딩·메모리·속도 문제 없음 실측 판단.

## v0.3.2 — 조밀화: 시작 3절·전적·전황·연타 + 전환마다 반드시 반응

"경기 시작 전·끝난 후 정밀한 반응, 중간 썰, 장면 바뀔 때마다 뭐라도" — 반영. 회귀 84건.

- **시작 3절**: 매치업 콜 → 관계/썰 → **상대 분석**("상대 정보 — 탄+대공 보유, 접근이 답",
  문헌 '확실' 태그만) → VS 글귀 반응까지 이어지는 오프닝 시퀀스.
- **끝난 후**: KO → 후속(완승/역전승/…) → 승패 화면 반응 → **오늘 전적**("오늘 3전 2승! 술맛
  나는 날이군") → 스토리 페이지 사담까지.
- **중간 썰**: 전황(격차 48+ — "크게 벌렸다! 몰아붙여라!"/"밀리는군…") · 반피 돌파(양쪽) ·
  연타(2.5초 3히트 — "연타다!") 신설, 교착 잡담 12→9초.
- **전환마다 반드시 반응**: 화면 멘트의 확률 게이트 전부 제거(유파·문구·스토리),
  대사가 쿨다운에 막힌 전환에도 **초상 팝+창 테두리 반짝(장면 비트)**은 항상.
  선택창 사담 방문당 3→4마디, 대기열 상한 3→4(길어진 오프닝 수용).
- 부하: 추가분 전부 이미 읽는 값의 비교 연산 — 실측 비용 0 계열.

## v0.3 — 도파민 업데이트: 완승·역전승·연승 + 붓글씨 + 본겜 폰트

전면 기획(장면 13종 × 이벤트 카탈로그) 1단계 구현. 회귀 **79건**.

- **본겜 폰트 싱크** — 해설창 글씨를 한글패치 조판에 쓴 그 폰트(**갈무리 8×8**)로.
  KSX1001 한글 2350자 서브셋 37KB를 **HTML에 임베드** — CDN·인터넷 불요, 픽셀 정수 크기로 또렷.
- **붓글씨** — 장 타이틀 붓글씨의 원본 서체(Noto Serif CJK KR Black)를 임팩트 대사 전용
  서브셋 98KB로 임베드. **KO·비오의·완승급 대사가 붓글씨로 꽝** 박힌다.
- **도파민 이벤트 10종** (리버싱 0 — 이미 읽는 값만으로):
  · **완승**: 무피해 KO — "완승이다!! 흠집 하나 없군, 핫핫하!!"
  · **역전승**: 빈사에서 승리 — "빈사에서 뒤집었다!! 이게 승부다!!"
  · **순살**: 10초 내 KO / **접전**: 60초+ 라운드 / **벼랑끝**: 양쪽 빈사
  · **선제타**: 매치 첫 유효타 / **선취·열세·매치포인트**: 라운드 전적 맥락 라인
  · **연승**: 세션 연승 2·3·5·7·10판 기념 / **설욕**: 직전에 진 상대를 꺾으면
  KO 후속 멘트는 역전승>완승>순살>설욕>연승 우선순위로 **한 개만** — 투머치토커 방지.
- **썰 풀 확장** — 선택창 사담 화자당 8~9종(로스터 썰), 카드·타이틀 사담 풀.
- **해설 기본 켬** — 처음 여는 사람도 바로 캐릭터 해설을 만난다. 끈 사람의 선택은 저장값으로 유지.
- **대사 굳음 방지**(제보) — 장면이 바뀌면 2초 넘은 옛 대사는 스르륵 사라지고,
  장면이 안 바뀌어도 8초 지나면 자연 소멸("…" 복귀). 새 대사가 오면 페이드는 즉시 취소.
- SP 토스트가 [기술명 숨김]·[발동 결과 표시] 설정을 따르게 수정(제보).
- "승부 있었다" 등 일본식 직역투 4건 교체 — 이후 대사 수칙으로 못박음.

## v0.2 — 새 넘버링: "HTML NGP SS2 runner — SP button · Character chat"

사용자 지시로 버전 넘버를 v0.2로 리셋 (구 v1.9.1 다음 판). 이번 판 변경:

- **해설창 가로폭 = 게임 디스플레이 가로폭** — 페이지 전폭이 아니라 실제 게임 그림 폭
  (NGPC 160:152 종횡비로 계산, 캔버스 요소는 전폭이라 rect를 믿으면 안 됨)에 맞추고 중앙 정렬.
  좌우 여백은 게임과 같은 검정 받침(#commRow). 리사이즈·부팅 시 재계산, 폭 맞춤(fitFont)도 연동.
- **전환 분류 요령** (사용자 아이디어): 메뉴 화면 전환은 **사용자가 누른 직후에만** 일어난다 —
  입력(터치·키보드) 타임스탬프와 대조해서, 클릭 없이 넘어간 전환은 데모·스토리류
  **특이 전환(게임 전후·스토리창)**으로 분류. 클릭 전환 = 화면 멘트(선택/유파/타이틀/카드),
  자동 전환 = **스토리 사담**("호오, 이야기가 이어지는군" / "……계속 봐라").
  승패 결과 반응 뒤 자동으로 넘어가는 연출 페이지(0↔2 반복)에도 스토리 사담.
- 회귀 **68건** 통과 (자동 전환 사담·결과 뒤 스토리 페이지·폭 정렬 검사 신설).

## v1.9.1 — 효과 봉인·선택창 사담·문구/승패 화면 반응 (사용자 피드백 3건)

- **화면 효과는 대화창에 국한** — 해설창 높이를 46px로 고정하고 overflow를 봉인했습니다.
  글자가 커져도(임팩트 26px) 게임 화면을 밀거나 흔들지 않습니다.
- **캐릭터 선택창 사담** — 고르는 동안 7초마다 화자별 사담(방문당 3마디):
  하오마루 "겐주로 녀석도 있군. 성격은 더러워도 검은 진짜다" / 갈포드 "나, 나코루루가 있다…!
  시, 신경 쓰지 마라!" (시리즈 공식 관계 범위만)
- **문구(VS) 화면 반응** — "한판 승부 정정당당히" 같은 전투 전 글귀 화면(리버싱: scr 0 ·
  mode 0xF1)에서 한 마디: "한판 승부… 정정당당히다!" / "…각오는 좋다. 간다"
- **승패 후 이름 화면 반응** — 이기거나 지고 캐릭터 이름/연출이 나오는 화면(전투 scr → 0/2
  전환 실측)에서 승패를 알고 반응: 승리 "봐라, 승자의 얼굴이다! 핫핫하!" /
  패배 "…저 이름, 기억해 둬라. 다음에 갚아주면 된다". 표정도 연동(기쁨/걱정).
  결과·문구 화면에서는 전투 잡담이 나오지 않게 가드.
- 회귀 **65건** 통과 (선택창 사담·문구 반응·승/패 화면 반응·높이 고정 검사 신설).

## v1.9 — 롬 초상·화면 코멘트·잡담·폭 맞춤 (사용자 피드백 4건)

- **롬 초상** — "화면에 등장 안 해도 나오게 안 되나. 추출해서 주소를 알면"(사용자 아이디어) 그대로:
  HUD 초상 타일(16×16, 4타일×16B)의 **롬 파일 오프셋 15캐릭터분을 리버싱**해서
  (검증환경 K1GE VRAM 덤프 → 롬 바이트 역검색, 캐릭터 셀렉트 화면 스크린샷으로 전원 신원 확정)
  배포물에는 **주소·팔레트·체크섬 숫자만** 넣었습니다. 그림은 로드된 사용자 롬에서 그 자리에서
  그립니다 — 롬을 넣는 순간부터 해설자 4인 전원이 진짜 초상으로 나옵니다(등장 대기 불필요).
  체크섬이 다른 롬(v0.96 등)이면 조용히 기존 수확/도트 방식으로 폴백. 두 롬(한글판·일어판)
  주소·바이트 동일 실측 확인.
- **대사 폭 맞춤** — 대사 가로길이를 해설창 폭에 맞춥니다: 짧은 말은 크게("간다!!" 26px),
  긴 말은 작게. 실제 DOM 측정 기반(픽셀 폰트 로드 전후 오차 없음), 클램프 11~26px.
- **화면마다 코멘트** — 기존 캐릭터 선택(2)·유파 선택(4)에 더해 **타이틀(0)·카드 화면(6)**에서도
  화자별 한 마디. 화면별 쿨다운 분리(한 메뉴 순회에 각각 말할 수 있게).
- **놀 때의 대책** — 전투 중 공방이 12초 끊기면 화자별 잡담("서로 노려보는군… 술이 식겠다" /
  "…정적. 나쁘지 않다"), 메뉴 한 화면에 15초 머물면 메뉴 잡담. 낮은 우선순위·긴 쿨다운이라
  실전 흐름은 방해하지 않습니다.
- 회귀 **60건** 통과 (타이틀·카드 조잘, 전투·메뉴 잡담, 폭 맞춤, 롬 초상 렌더 검사 신설.
  13.2 KO 라인 검사의 수집 방식 보강 — 직전 큐 꼬리에 가려 놓치던 타이밍 결함 수정).

## v1.8 — 해설자가 캐릭터가 됐습니다

- **TTS 폐지** — 한국어 TTS의 한계(기계음·기기 편차)로 음성을 뺐습니다. 자막 전용.
- **상시 해설창** — 게임 화면 **위에** 해설창이 고정으로 자리합니다(레이아웃 분리 — 화면·체력바를
  가리지 않음). 픽셀 폰트(둥근모꼴, CDN·오프라인 폴백)로 게임과 같은 결.
- **화자 = 캐릭터** — 해설자를 하오마루·나코루루·한조·갈포드 중에서 고릅니다. 각자의 말투로
  중계하고, 등장 캐릭터와의 **관계**가 대사에 드러납니다: 하오마루가 겐주로전을 보면
  "겐주로냐…! 네놈과는 몇 번을 겨뤄도 좋지", 갈포드가 나코루루전을 보면
  "나, 나코루루다…! 머, 멋진 모습을 보여야 해!" (관계는 시리즈 공식 설정 범위만)
- **도트 초상 + 표정 변화** — 자체 제작 도트 초상(롬 자산 아님)이 상황 따라 표정을 바꿉니다:
  잘 풀리면 기쁨, 맞거나 위기면 걱정, 2초 뒤 평상 복귀.
- **기술명 표시/숨김** 설정 — 숨기면 "적중이다!"처럼 이름 없이 말합니다.
- **진짜 초상 수확** — 게임 초상은 배포물에 넣을 수 없으므로(저작권), **사용자 롬에서 런타임에**
  가져옵니다: 해설자 캐릭터가 전투에 등장하면 화면 HUD 초상을 잘라 캐시(롬별 저장).
  그 전까지는 자체 도트, 등장한 뒤부터는 진짜 초상. 표정은 밝기·채도 필터와 떨림으로.
- **글자가 연기합니다** (TTS 대신): 타이핑 등장(픽셀 게임 감성) · 큰 히트 펀치 팝 ·
  비오의/역전 흔들림 · 위기 파르르 떨림 · KO는 즉시 임팩트(대문자·패널 플래시) ·
  KO/큰 히트에 짧은 진동(지원 기기).
- 회귀 54건 (기술명 숨김 검사 신설, 화자 대사 반영, 끄기 시 타이핑 정지 포함).


## v1.7 — 해설 컨셉 개편: 분위기 메이커 (사용자 피드백)

"투머치토커다. 기술명을 꼭 외칠 필요가 없다. 분위기를 타고 즐겁게 해주는 게 중요하다."

- **모드 폐지** — 입문/캐스터/분석/핵심만 4모드를 없애고 해설은 **끔/켬 하나**.
- **말을 줄였습니다** — 기술명은 **결과가 있을 때만** 부릅니다("셰드 무사 적중!").
  발동 단독·잔히트·잔피격은 침묵. 교육성 설명(유파·카드·기상 강좌)은 전부 뺐습니다.
  이 게임은 공방이 빠르고 짧습니다 — 문장도 짧게.
- **캐릭터 썰** — 시작 입털기에 시리즈 설정 한 줄: "쥬베이 — 야규의 검호, 두 자루 칼".
  (재미용 설정 이야기이며 게임 플레이 사실 단정이 아닙니다)
- **말투 선택** — 캐스터(존댓말) / 친구(반말 "개맞았어!!"). 설정에서 전환.
- **음성 크기** — 작게/보통/크게. "소리가 좀 크다" 피드백으로 기본을 보통(70%)으로.
- **목소리 선택** — 기기에 한국어 TTS가 여러 개면 고를 수 있습니다.
- GPT 4차 검수 마무리 반영: 음성 끄기 즉시 정지, 미검증 차단을 kbOf 런타임에도 이중으로,
  회귀 기본 페이지 index.html(SS2_PAGE), 문서 표기 정리.
- 회귀 **53건** ×2 통과 (발동 단독 침묵 검사 추가).


## v1.6.2 — 3차 검수 반영 (릴리스 차단 오류 4건 수정)

- **음성 켜기 버튼 오류 수정** — 옛 함수명(say) 호출이 남아 클릭 시 코드 오류가 나던 것.
- **진짜 단일 큐** — 음성이 켜져 있으면 앞 문장이 끝나야 다음을 꺼냅니다. 자막과 음성이
  항상 같은 문장을 유지합니다. 대기 상한 3, 가득 차면 가장 낮은 우선순위를 버립니다(문서에 명시).
- **결과 처리 순서 정리** — 더블 KO > 기술 KO > 기술 다운(히트 포함, 한 문장) > 기술 적중 >
  일반 다운. "다운 문장 뒤에 히트 문장이 또 나오던" 이중 발화 제거.
- **해설 끄기 = 예약 정리** — 끄는 순간 대기열·음성·자막·결합창을 전부 비웁니다.
- **시작 멘트 실버그 수정** — 전투 진입 틱에 체력 변화가 없으면(메뉴에서 128/128 그대로)
  시작 멘트를 통째로 건너뛰던 구조 결함. **합성 회귀 14.6이 잡아냈습니다.**
- 역전 카운트는 실제 발화된 것만 소모. 첫 다운에만 기상 옵션 설명(앞·뒤 이동 기상,
  A·B 기상 공격 — 공략 2종 교차 확인), 이후엔 "다운입니다"로 짧게.
- 지식 임베드에 미검증 차단 필터 내장(수동 삭제 아님), 괄호 균형 정리(시크루 타기).
- 회귀 **52건**: 합성 7건(결합·더블KO·미검증 침묵(입문)·역전 상한·음성 켜기·시작 3연·끄기 정리),
  페이지 URL `SS2_PAGE` 환경변수화.


## v1.6.1 — 2차 검수 반영

- **미검증 기술 침묵**: 실기 미검증 7기(레피 슐·시게츠·오보로가타나·쿠게 등)를 지식 임베드에서
  제외 — 발동하면 이름은 호명하되(발동 자체가 검증) 문헌 설명·태그는 말하지 않습니다.
- **동명 기술 충돌 수정**: 첫 호명 기록 키에 유파를 포함 (화염·간헐돌·그림자분신 등).
- **역전 라운드당 2회 제한** (기존: 쿨다운만).
- **손 커맨드 비오의**도 비오의로 대접합니다 (학습표로 인식된 경우 포함).
- **다운 멘트에 기상 옵션**: 방향 기상·A/B 기상 공격 — 공략 문헌으로 확인 후 반영.
- **해설 로직을 순수 단계 함수로 분리**(commStep) — 합성 램 값으로 롬 상태와 무관하게 검사.
  회귀에 합성 검사 4건 추가 (발동+히트 결합 · 동시 KO · 미검증 침묵 · 역전 폭주 없음), 총 49건.
- regress: 크로미움 경로 `SS2_CHROMIUM` 환경변수화, 재현 조건 헤더 명시
  (롬·세이브스테이트는 금지선이라 배포본에 없음 — 의도된 제약).


## v1.6 — 해설 재설계: 알림에서 중계로 (외부 검수 반영)

"이벤트 알림을 TTS로 읽는 단계"라는 검수를 받고 구조를 갈았습니다.

- **기술과 결과를 한 문장으로**: 발동을 0.45초 들고 있다가 그 사이 결과가 오면 합칩니다 —
  "셰드 무사, 맞췄습니다!" / "호월참으로 다운!" / "비오의 적중! 그대로 KO!!"
  투사체처럼 늦게 맞으면 이름 먼저, 결과는 뒷문장으로.
- **자막·음성 단일 대기열**: 같은 스케줄러가 같은 문장을 자막과 음성에 보냅니다.
  낮은 우선순위는 버려지지 않고 줄을 섭니다(상한 2). KO급만 줄을 비우고 즉시.
  긴 문장은 오래 표시하고, 자막은 2줄까지 줄바꿈(모바일 잘림 해결).
- **거짓말 제거**: 지식 베이스에서 문헌 메모·추정 표현("가불로 보임" 등)을 정제해 음성에
  올라가지 않게 했고, 검증 안 된 단정(기상 사양, 캔슬 프레임 즉시 단정)을 문장에서 뺐습니다.
  캔슬 멘트는 "캔슬 입력!", 상대 소개는 성향 단정 대신 **보유 사실**("탄을 갖고 있습니다")로.
- **화면/음성 표기 분리**: 음성은 "KO"→"케이오", 괄호·카드번호를 정리해 읽습니다.
  취소된 발화의 콜백이 상태를 덮지 않게 발화 토큰을 두었습니다.
- **동시 KO**: "더블 KO!!" — P2 먼저 검사해 승리로 오판하던 것 수정.
- 전문 모드 이름을 **분석**으로. 라운드 리셋 때 KO 쿨다운 잔존 방지.
- 회귀에 **해설 검사 2건 추가** (총 45건), 롬 경로는 `SS2_ROM` 환경변수로.


## v1.5.1 — 해설 다듬기 (검수 반영)

- **멘트 과속 방지**: 낮은 우선순위(호명·히트·다운)는 직전 멘트 1.2초 안이면 통째로 생략.
  음성도 같은/낮은 우선순위는 말 중간에 끊지 않습니다. KO·비오의·역전은 항상 나갑니다.
- **손 커맨드도 알아봅니다**: 액션ID→기술명 표는 롬 버전마다 달라 하드코딩하지 않고,
  이 롬에서 원버튼으로 낸 기술의 액션ID를 **그때그때 학습**합니다(롬별 저장).
  한 번이라도 원버튼으로 낸 기술은 이후 손으로 커맨드를 넣어도 이름으로 호명됩니다.
- **캐릭터 시트에 기술 성격 표시**: 각 기술 줄 아래에 성격 태그와 설명 한 줄
  ("투사체 · 활을 꺼내 쏜다") — 해설과 같은 지식 표를 씁니다.
- **전투 시작 = 매치업 입털기**: 상대 캐릭터 오프셋(0x1D51)을 새로 리버싱해서, 시작 때
  "아수라 대 쥬베이! 자, 갑니다!" 하고 1.8초 뒤 상대 성향 한 줄을 붙입니다 —
  "받아치기를 가진 상대 — 함부로 치면 되받아칩니다" (문헌 지식의 태그 집계).
  라운드 간막이(1.9초)와 구분해서 시작과 라운드를 다르게 말합니다.

## v1.5 — 해설이 기술을 안다 (두 층위 해설)

해설 모드가 **입문 / 캐스터 / 전문 / 핵심만** 네 모드가 됐습니다 (⚙설정 → 해설, 음성은 별도 토글).

핵심은 **기술 지식 베이스**입니다. 해외 공략 문헌에서 기술 하나하나의 성격(투사체·대공·돌진·
잡기·순간이동·반격·빙결·기절·혼란…)을 수집해 (유파, 커맨드, 버튼) 3중 키로 롬 데이터에
접합했습니다 — 171기 중 137기, 그중 서술이 확실한 126기만 임베드했습니다. 추측은 말하지 않습니다.

- **입문**: 고유명사엔 설명이 따라옵니다 — "셰드 무사 — 활을 꺼내 쏜다". 같은 기술은 첫
  호명에만 설명하고 이후엔 이름만. 분노 폭발·카드 기술·반격기는 처음 나온 순간 한 번 설명합니다.
- **캐스터**: v1.4의 호명+리액션.
- **전문**: 전술·프레임 정보 — 잡기 사거리 밖("32 안쪽에서만"), 캔슬("캔슬 창 22프레임 고정"),
  이동기·설치기 코멘트. 잔히트엔 침묵합니다.
- **핵심만**: KO · 비오의 · 역전만.

전 모드 공통으로 **역전 감지**(체력 리드가 뒤집힌 순간)가 추가됐고, 음성에 우선순위가 생겨
KO 멘트가 히트 멘트에 끊기지 않습니다.

검증: 실롬 브라우저 테스트 — 입문 1회차 설명/2회차 이름만, 역전, 핵심만 모드 침묵 규칙,
페이지 에러 0. 기존 회귀 43건 통과.


## v1.4 — 행동 가능 게이트 + 해설 모드

라운드 시작 연출·피격·다운·필살기 경직 중에 SP를 누르면 게임이 커맨드 앞부분을 버리고
꼬리의 →+A만 받아 **전진 기본기가 오발**되던 것을 막았습니다. 코어판 v13에서 실게임
실측으로 원인을 잡은 규칙을 그대로 옮긴 것입니다.

막힌 입력은 버리지 않습니다 — 행동 가능해지는 순간 발동합니다(선입력 버퍼, 최대 1.5초).
자기 기본기 중에는 즉시 주입해 게임의 킬캔슬 창(+22프레임)을 그대로 살립니다(v14 규칙).

검증: 실롬 브라우저 테스트로 ① 평시 즉시 발동(회귀 없음) ② 필살기 경직 중 입력이
선입력으로 걸렸다가 자동 발동 ③ 잔재 없음, 그리고 기존 회귀 43건 전부 통과.

### 해설 모드 (⚙설정 → 해설)

전황을 읽어 자동으로 해설합니다 — 기술 발동(기술명 호명), 히트 크기, 다운, 체력 경고,
KO, 라운드 시작. **자막**과 **자막+음성**(브라우저 내장 한국어 TTS) 중 고를 수 있고
기본은 끔입니다. 상대 기술은 이름 표가 롬 버전마다 달라 일반 문구로만 알립니다.

비용: 0.1초마다 램 몇 바이트 읽기가 전부입니다. 실측 60.7 → 60.0 fps — 차이 없음.
getState 직렬화는 쓰지 않습니다(전투 중 금지 원칙 그대로).


## v1.3 — [콤보] 버튼 폐기

약베기로 열고 상대 HP가 줄면 마무리 기술로 캔슬하는 버튼이었습니다. 없앴습니다.

25ms마다 P2 체력을 폴링해야 했고, 힙을 못 잡으면 히트컨펌 감시를 통째로 건너뛰었습니다.
가드당하거나 빗나가면 350ms 뒤 그냥 멈추는데 그 사이 시작타 경직은 이미 진 상태입니다.
기술은 SP 자리 7개와 SP 버튼에 다 걸려 있으니 기본기는 [A]로 직접 치는 편이 낫습니다.

같이 사라진 것: 캐릭터 시트의 [콤보] 탭, 약베기/강베기 선택, 키보드 V.
[SP 배치]는 탭이 하나뿐이라 탭 줄 없이 바로 열립니다.

## v1.2 — 기본 배치 갱신

SP+방향 기본 배치를 코어판·NGP판과 같은 표로 맞췄습니다. 대각선(↘·↙)과 공중까지
채워 131 → 157자리. 중립 자리는 항상 기술 1번이라 SP1과 방향 없이 누른 SP가 같습니다.


버전 표기: **0.5가 첫 공개 배포판**입니다. 그 이전의 v10.x / v1.x는 공개 전 내부 개발 리비전이며,
코드 주석과 인계 문서에 근거 기록으로 남아 있습니다.

## v1.0 — 정식 배포판

### 터치 중 끊김의 원인을 찾아 고쳤습니다

EmulatorJS가 WebGL 컨텍스트를 `alpha:true` · `premultipliedAlpha:true`로 만들고 있었습니다.
브라우저 합성기가 매 합성마다 캔버스를 페이지 배경과 블렌딩하는데, 터치 중에는
합성기가 이미 바빠서 이 한 겹이 에뮬레이터 루프를 굶깁니다.

`getContext`를 감싸 불투명으로 강제 — **디패드 드래그 51.8 → 59.6 fps, 끊김 65 → 3회.**
화면 필터가 무겁던 것도 상당 부분 같은 원인이었습니다("부드럽게" 49.1 → 59.5 fps).

### 그 밖에

- 전역 `addEventListener` 패치를 **부팅 2초 후 원본으로 복원**
  (영구 패치면 나중에 붙는 리스너까지 조용히 passive가 됩니다)
- 로더 실패 안내를 원인별로 분리 (연결 / CDN 차단 / 새로고침), 20초 무응답 감지 추가
- 파비콘 · 테마 컬러 · OG 태그 · 홈 화면 추가 메타 정리

## v0.6 — 외부 검수 반영

- **SP 버튼 아래 기술명 표시 제거.** `refreshSpLabel()`이 `setDirs()`에서도 불려서
  **디패드를 한 칸 움직일 때마다** RAM 읽기 + 객체 생성 + DOM 쓰기가 돌고 있었습니다
- **강 판정의 `box-shadow` 제거** → 배경·테두리 색 변경으로 대체.
  하필 버튼을 누르고 있는 동안 켜져서 모바일 GPU에 계속 합성을 시켰습니다
- ⚙설정에서 **[SP 연속기] 제거** — 실제로는 발동 후 A만 눌러도 이어집니다. 항상 자동 완주
- ⚙설정에서 **[콤보 마무리] 제거** — [캐릭터] → [콤보] 탭에 고르는 칸이 이미 있었습니다
- **접근성** — 다섯 버튼 지름 동일(0.170u, 360px 폭에서 61px), A·B를 26°/70°로 벌려
  인접 간격 0.198u → 0.225u

## v0.5 — 첫 공개 배포판

- **프레임 보정(자동 프레임스킵) 완전 삭제** — 연타 시 끊김의 직접 원인이었습니다
- 버튼 스킨 5종 · CSS `transition` 전부 제거 (버튼 애니메이션 off)
- 그라디언트·그림자를 선언 자체에서 제거 — 이제 **색 교체만** 합니다
- 기본 버튼 배치를 폰 비율에 맞춰 재조정 (360×800 ~ 430×932 실측)
- **불러오기 중 매크로 정지** — 기술이 나가는 도중 상태를 불러오면 입력 찌꺼기가 남던 문제
- 안내 문구 정리
