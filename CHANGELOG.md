# 변경 이력

## v0.5.9 — 가로 모드가 제대로 서고, 상단 줄이 안 잘립니다

제보: "가로모드하면 레알 저렇게 되나?" / "상단 메뉴가 잘리네, 세로모드 때 칸이 부족하네" /
"촬영모드 꺼져 있으면 드래그 편집할 때도 꺼내놓지 마라."

**① 가로 모드 — 게임이 0~47px로 짜부라져 있었습니다**

조작판이 세로 기준으로 **고정 300px**을 먹고 있었습니다. 세로에선 아래 절반이 조작판이니 맞는데,
가로에선 화면 높이 자체가 360~460px이라 **남는 높이가 0~47px**이었습니다(실측:
900×420 게임 7px / 780×360 게임 0px). 보신 그 화면입니다.

- **가로에서는 조작판을 게임 위에 얹습니다.** 흐름에서 빼내 화면 전체를 덮게 하고 배경을 없앴습니다.
  게임이 307px(900×420) · 247px(780×360)로 살아납니다 — 7px에서 44배입니다.
- **팔 길이만 줄입니다.** 그대로 두면 가장 먼 SP가 해설 띠까지 올라갑니다. 버튼 지름은 그대로 두고
  팔 길이를 역산해 **SP 윗면이 해설 띠 바로 아래**에 서게 했습니다. 버튼은 74~80px을 유지합니다.
- **[▶▶] [기술] [Option] 을 해설 띠 아래 왼쪽으로 내렸습니다.** 8px에 두면 상단 줄 뒤에 깔려
  아예 못 누릅니다.
- 세로 배치는 **한 픽셀도 안 건드렸습니다.**

**② 상단 줄이 잘리던 것**

캐릭터 버튼이 「카즈키 · 나찰」처럼 길어지면 혼자 자리를 다 먹어 **⚙·⛶ 가 화면 밖으로** 나갔습니다.

- 좁은 화면(470px 이하)에서는 **유파를 떼고 이름만** 답니다. 유파는 [캐릭터] 시트에 바로 있습니다.
- 이름 칸을 22vw(작은 폰 19vw)로 물리고, 간격·글자를 조여 **340px 폭에서도 8개가 다 들어옵니다.**
  가장 긴 이름(샤를로트·나코루루)으로 확인했습니다.
- 그래도 넘치면 가로로 밀려 나오게 뒀습니다(잘려서 사라지지 않게).

**③ 촬영 자막**

- **촬영 모드가 꺼져 있으면 배치 편집에서도 안 꺼냅니다.** 켜 두고 편집에 들어가야 손잡이가 나옵니다.

- **가로 버튼 배치를 조밀한 마름모로** 바꿨습니다. 세로용 부채꼴을 가로에 그대로 쓰면
  네 버튼이 오른쪽에 흩어져 손이 계속 움직여야 합니다. 가로에선 화면이 넓어 코너를 축으로 삼을
  이유가 없어, **한 자리에서 엄지만 까딱하는 마름모**로 모았습니다.
  이웃 간 중심거리는 지름의 1.19배 — 겹치지 않으면서 가장 붙는 값입니다.
- **README 그림을 갈아 끼웠습니다.** 무거운 클립 3개(10.6MB)를 빼고, 실기 움짤에서 고른
  **9개(3.4MB)** 로 바꿨습니다. 저장소가 12.7MB → 5.7MB 로 줄었습니다.

회귀 124건 통과.


## v0.5.8d — 더미의 "이상하게 멀어짐" 정정 (제 잘못이었습니다)

제보: "더미가 제대로 작동 안 하더라. 이상하게 멀어지고 하기는 하는데."

**제가 넣은 「제자리 고정」이 원인이었습니다. 통째로 들어냈습니다.**

`0xE38`·`0xE78` 을 캐릭터의 좌표라고 보고 상대 좌표를 물어 뒀는데,
**이 값들은 월드 좌표가 아니라 카메라(화면) 상대 좌표**였습니다.
아무 입력도 안 준 채로 두면 P1 값이 저 혼자 99→79→62→45→25 로 흘러갑니다 —
P1은 가만히 있는데 값이 변합니다. 카메라가 움직인 것입니다.

그러니 그 값을 고정하면 **상대를 화면의 한 점에 묶어 두는 꼴**이 되고,
카메라가 스크롤할 때마다 상대가 스테이지를 가로질러 끌려가며 이쪽을 밀어붙입니다.
보신 "이상하게 멀어짐"이 그것입니다. 실측으로도 고정은 애초에 성립하지 않았습니다 —
매 프레임 써 넣어도 231→215→199 로 흘렀고, 그 폭이 P1 쪽 이동폭과 정확히 같았습니다.

- **좌표는 이제 건드리지 않습니다.**
- **라운드 종료 연출 중(국면 3)에는 아예 손대지 않습니다.**
- 반격을 막는 부분(COM 판단 바이트)은 그대로입니다 — 7개 전투 상태에서 피해 0.

**지금 실제 동작**: 더미를 켜면 상대가 **한 번 뒤로 물러나 구석에 서고, 그 뒤로는 안 움직입니다.**
(거리 80 → 144에서 멈춤, 관측 내내 유지.) 다가가서 찍으시면 됩니다.
멈춰 세운 게 아니라 **판단을 막은** 것이라, 물러나는 그 한 번은 남습니다.

회귀 124건 통과.


## v0.5.8c — 촬영 자막을 배치 편집의 정식 구성원으로

제보: "촬영 오버레이가 드래그가 잘 안 됨. 그냥 정식에 넣어도 되겠다."

**원인은 `touch-action` 이었습니다.** 자막에 이 한 줄이 없어서, 손가락을 대면 브라우저가
스크롤 제스처로 먼저 채 가고 `pointercancel` 이 날아와 드래그가 중간에 끊겼습니다.
화면 버튼들은 `#controls` 에 이미 걸려 있어 무사했고, **자막만 그 밖에 있어서** 걸렸습니다.

- **자막도 버튼과 똑같이 끌립니다.** `[설정 → 버튼 배치 → 드래그로 배치 편집]` 에 들어가면
  버튼들과 함께 점선이 둘러지고, 잡아서 원하는 자리에 놓으면 됩니다.
- **촬영 모드가 꺼져 있어도 자리를 미리 잡아 둘 수 있습니다.** 편집에 들어가면 견본이 뜹니다
  (「촬영 자막 — 끌어서 옮기세요」). 나중에 켜면 그 자리에 나옵니다.
- **화면 밖으로 밀어도 안으로 물려 들어옵니다.** 한 번 놓치면 다시 못 잡으니까요.
- 잡는 자리를 넓히고 편집 중에는 배경을 깔아 어디를 잡아야 하는지 보이게 했습니다.
- `[배치·크기 초기화]` 로 버튼과 같이 제자리로 돌아옵니다.

회귀 124건 통과.


## v0.5.8b — 더미 상대

요청: "앞에 더미를 둘 수 있나?"

시연 클립을 찍는데 COM이 계속 반격하면 기술 하나를 온전히 담을 수가 없습니다.
설정에 **[더미 상대]** 를 넣었습니다. 기본은 끔.

- **상대가 반격하지 않습니다.** 7개 전투 상태(아수라·갈포드·겐주로·하오마루·주베이·우쿄·아수라 벨제붑)
  에서 25초씩 재 봤습니다 — 더미를 끄면 9~33 깎이고, 켜면 **전부 0**입니다.
- **제자리에 섭니다.** 판단만 막으면 상대가 슬금슬금 구석으로 물러납니다(거리 80→144 실측).
  가로 좌표를 물어 둡니다. 단 **맞는 중에는 놓아 줍니다** — 맞고도 안 밀리면 타격이 들어간 것처럼
  안 보이니까요.
- **쓰러지지 않습니다.** 체력이 바닥나기 직전에만 다시 채웁니다. 늘 만피로 묶으면
  체력 막대가 아예 안 움직여 타격이 들어갔는지 확인이 안 됩니다.
- **라운드는 평소대로 넘어갑니다.** 150초를 돌려 라운드 국면이 정상 순환하는 것까지 확인했습니다.

### 어떻게 찾았나 (되돌리지 말라고 적어 둡니다)

처음엔 **상대의 액션ID를 서 있기로 되돌렸습니다.** 반격은 확실히 사라졌는데
**라운드가 끝날 때 게임이 멈췄습니다** — 시간 초과 시 상대가 취해야 할 자세까지 덮어써서
종료 연출이 진행되지 못한 겁니다. 종료 15초 전에 손을 떼도 똑같이 멈췄습니다.

그래서 상대 구조체를 전수로 훑었습니다(224자리 × 두 값 × 각 15초).
**0xE88** 한 자리가 나왔습니다. 값 256개를 전부 재 보니 **비트0·비트1이 둘 다 선 값 16개**가
전부 같은 결과 — 그 두 비트가 원인입니다. P1 쪽 같은 자리(0xE48)는 항상 0이라
사람이 조종하는 쪽은 안 쓰는 AI 전용 자리로 보입니다.

회귀 119건 통과.


## v0.5.8 — 촬영 모드: 한 버튼이 무슨 커맨드인지 자막으로

요청: "요런 컨셉으로 몇 개 만들 건데 가독성 크게 만들기용으로 어떻게 안 될까. 커맨드를 자막으로 뿌려 준다거나."

**플레이용이 아니라 녹화·스샷용입니다.** 기본은 꺼져 있습니다.

- 기술이 나갈 때 화면 아래에 **[누른 것] ▶ [실제 커맨드]** 와 기술 이름이 큼직하게 깔립니다.
  「← + SP ▶ ↓ ↘ → ＋ A · 나바이베루」 — 한 버튼이 무엇으로 풀리는지가 한 컷에 담깁니다.
- **글자에 불이 들어오는 시점이 실제 입력 시점입니다.** 연출로 순서를 흉내 낸 게 아니라,
  컴파일된 입력 스텝마다 자막 글자 번호를 달아 두고 그 스텝이 패드에 들어갈 때 켭니다.
- 크기는 **[켬] / [크게]** 둘. [크게]는 세로 480p 로 줄여도 읽히는 크기입니다.
- **표기는 기술표와 같은 정규형(↓↘→)** 으로 냅니다. 왼쪽을 보고 있으면 실제 입력은 좌우가
  뒤집히지만, 자막까지 뒤집으면 같은 기술이 클립마다 달라 보입니다.
- 밝은 스테이지에서 흰 글자가 묻히지 않도록 **가장자리로 사라지는 그늘**을 깔았습니다.
  사각 판을 깔면 캡처에서 UI처럼 보여 자막의 맛이 죽습니다.
- **자막도 끌어서 옮깁니다.** [설정 → 버튼 배치 → 드래그로 배치 편집] 에 들어가면 견본이 뜨고,
  버튼과 같이 원하는 자리에 놓을 수 있습니다. [배치·크기 초기화] 로 같이 돌아옵니다.
- 자막 위를 눌러도 그대로 게임에 들어갑니다(입력을 가로채지 않습니다).

회귀 114건 통과.


## v0.5.7c — [설치] 버튼이 항상 보입니다

제보: "설치 버튼 계속 나오게 해줘."

**브라우저가 설치 신호를 안 보낼 때가 많습니다.** 한 번 무시했거나, 아이폰이거나,
브라우저 나름의 판단으로 안 보내면 버튼이 아예 안 떴습니다. 그러면 "설치를 어떻게 하냐"가 됩니다.

- **버튼을 항상 띄웁니다.** 이미 설치된 상태(홈 화면에서 실행 중)일 때만 숨깁니다.
- **시작 화면에도 [📲 홈 화면에 설치]** 를 뒀습니다. 상단 줄은 게임이 뜬 뒤에만 보이니까요.
- **설치 신호가 없으면 그 브라우저에서 손으로 하는 법**을 알려 줍니다 —
  삼성 인터넷은 「≡ → 현재 페이지 추가 → 홈 화면」, 크롬은 「⋮ → 홈 화면에 추가」,
  아이폰은 「공유 → 홈 화면에 추가」, PC는 주소창의 설치 아이콘.
- 파일로 직접 연 상태(`file://`)면 그렇다고 알려 줍니다 — 설치는 https 에서만 됩니다.
  받아 둔 html 파일은 **인터넷 없이 혼자 도는 용도**고, 홈 화면 설치는 웹 주소로 열었을 때만 됩니다.
  안내문에 들어갈 주소를 그대로 적어 두었습니다.
- **판본 표시가 낡아 있던 것**을 고쳤습니다. 시작 화면과 탭 제목이 계속 `v0.5.6` 이었습니다 —
  앞선 일괄 치환이 `v0.5.6d` 만 잡고 `v0.5.6` 은 지나쳤습니다.

회귀 108건 통과.


## v0.5.7b — 기술명 알림 폐지 · 단말 프리셋 · 브라우저 안내

- **기술명 하단 알림을 기능째 없앴습니다.** 해설 띠가 이미 「비오의냐! 좋다, 와라!!」 하고 말해 주는데
  화면 아래에서 같은 순간 「←+A+B  비오의」가 또 뜨는 건 겹치는 소리였습니다.
  카드 없음·저장 같은 **시스템 안내는 그대로** 뜹니다.
- **[기술명] 설정도 없앴습니다** — 항상 켬. 끄면 「적중이다!」처럼 밋밋해질 뿐이라 고를 이유가 없었습니다.
  설정이 14개에서 **12개**로 줄었습니다.
- **단말 프리셋** — 처음 켤 때 기기를 보고 버튼·패드 크기와 투명도를 자동으로 맞춥니다.
  폰 / 태블릿 / 작은 게임기 화면 / PC 넷입니다. **저장된 설정이 있으면 절대 건드리지 않습니다.**
- **시작 화면 한 줄 안내** — 갤럭시에서 크롬으로 열면 삼성 인터넷을 권하고,
  PC로 열면 키보드 키를 알려 줍니다(방향키 · 1=SP · Z=A · X=B · C=A+B).

회귀 108건 통과.


## v0.5.7 — 기본 배치 · 화자 성격 · 아트웍 손보기

**조작**

- **기본 버튼 배치를 바꿨습니다.** SP가 가장 바깥 위, A+B가 그 왼쪽 아래. 결과는
  SP(위)·A+B(왼)·B(오른)·A(아래)의 마름모입니다. A·B 자리는 그대로 두어 손 기준점이 안 흔들립니다.
- **[하단 알림] 설정을 없앴습니다.** 끌 이유가 없는 자리였습니다 — 기술명을 감추고 싶으면
  [기술명] 설정이 이미 그 일을 하고(그러면 토스트도 같이 침묵), 카드 없음·저장 같은 알림은 꺼서 좋을 게 없습니다.

**해설 — 나코루루와 한조가 재미없다는 지적을 받아**

- **나코루루가 유파를 타고 인격이 갈립니다.** 수라면 우리가 아는 상냥한 나코루루,
  **나찰이면 서늘하고 말이 짧아집니다.** 같은 사건이라도 다른 사람이 말하는 것처럼 들립니다.
  - 역전 — 수라 「바람이 이쪽으로 불어요…!」 / 나찰 「…흐름이 돌았군」
  - 위기 — 수라 「위험해요… 조심하세요!」 / 나찰 「…피 냄새가 난다. 네 것이다」
  - 역전승에서는 존댓말이 한 번 무너집니다 — 「이겼다…! 아, 죄, 죄송해요. 저도 모르게…」
- **한조에게 아들 신조와 주군 도쿠가와가 생겼습니다.** 지금까지는 일어난 일을 명명하기만 했습니다.
  - KO 「…끝이다. 시체는 밤이 치운다」 / 「…승부가 났다. 도쿠가와의 이름에 흠은 없다」
  - 패배 「…이대로는 신조를 볼 낯이 없다」
  - 완승 「…흠 하나 없군. ……호오」 — 늘 저음이라 한 번의 감탄이 더 셉니다
  - **다운 같은 잔사건에서는 아예 침묵합니다.** 말수가 적어야 결정적일 때 무게가 실립니다

**양옆 아트웍**

- **🎴 버튼이 [아트] 글자 버튼이 됐습니다.** 무슨 버튼인지 모르겠다는 지적을 반영했습니다.
- **길게 누르면(0.6초) 지금 화면을 아트로 담습니다.** 카드 화면을 안 지나는 모드도 있으니
  원할 때 직접 담을 길을 뒀습니다.
- **카드를 아직 못 모았어도 액자가 비지 않습니다** — 그 판 스테이지를 흐림 없이 겁니다.
- **스테이지 배경이 또렷해졌습니다.** 흐림 7→3.5px, 밝기 .55→.72. "뭔가 있긴 한데 안 보이는" 상태였습니다.

회귀 108건 통과.

## v0.5.6d — 뒤+A+B 비오의는 A+B 버튼에 불이 들어온다

제보: "뒤+A+B 하면 SP도 같이 클릭되는 효과가 보여서 거슬린다."

비오의를 A+B 로 내는데 **불빛은 SP 버튼에** 들어왔습니다. 발동 함수가 눌린 버튼을
`#spBig` 으로 못 박아 두고 있었습니다. 하단 알림도 「←+SP」로 떴고요.

- 어느 버튼에 불이 들어올지는 **부른 쪽이 정하도록** 바꿨습니다. 뒤+A+B 는 A+B 버튼에만 불이 들어옵니다.
- 하단 알림도 「←+A+B  비오의」로 바뀝니다.
- SP로 낸 기술은 전과 같이 SP 버튼에 들어옵니다.

회귀 108건 통과.

## v0.5.6c — 준비 화면마다 나오던 「천천히 골라라」 · 조사 (으)로

**한 판 끝나고 다음 준비할 때마다 메뉴 잡담이 나오던 것을 고쳤습니다.**

제보: "승리나 패배 후 캐릭 이름 나오고 다음 넘어갈 때 '천천히 골라라' 같은 말이 항상 나온다."

원인은 타이머였습니다. 메뉴 잡담은 *한 화면에 15초 이상 머무를 때* 나오게 돼 있는데,
그 타이머를 **화면이 바뀔 때만** 다시 셌습니다. 그런데 승패 이름 화면은 아직 전투 상태라
화면 전환 리셋이 안 걸리고, 그 뒤 조용히 전투만 빠져나오니 **전투 전에 세워 둔 낡은 타이머가
이미 만료돼 있었습니다.** 그래서 메뉴에 들어서는 순간 "15초 넘게 머물렀다"로 판정돼 매번 터졌습니다.

- **전투를 벗어나는 순간 타이머를 다시 셉니다.** 이제 실제로 그 화면에 오래 머물러야 나옵니다.
- **결과 뒤 10초 침묵(hush)을 메뉴 잡담·선택창 사담에도 적용**했습니다. 두 겹으로 막습니다.
- 코어판에도 같은 고침을 넣었습니다.

**조사 (으)로**

- 「츠바메 가에시**으로** 눕혔군!」 → 「가에시**로**」. 받침 판정을 넣었습니다.
  받침이 없거나 ㄹ 받침이면 「로」, 그 밖엔 「으로」. (「호월참으로」·「백귀살로」 모두 정상)

회귀 106건 통과.

## v0.5.6b — 귀신터치 2차 · 카드 캡처 안정화

**SP가 저절로 눌리는 것 — 라운드 중에도 났다는 제보를 반영해 다시 고쳤습니다.**

앞선 고침은 **판이 끝났을 때**만 막았습니다. 원인의 본체는 따로 있었습니다:
SP를 눌러도 지금 못 내면 **최대 1.5초까지 예약**해 뒀다가 낼 수 있게 되면 냈습니다.
그 사이 사용자는 이미 다른 걸 하고 있으니, 뒤늦게 나오는 기술은 전부 저절로 눌린 것처럼 보입니다.

두 겹으로 막았습니다.

- **예약 창을 1.5초 → 0.4초**로 줄였습니다(공중 재시도 1.2초 → 0.7초, 착지 대기 1.2초 → 0.67초).
  격투게임 선입력 관례(10~20프레임) 안쪽입니다.
- **그 사이 새 입력이 하나라도 들어오면 즉시 버립니다.** A·B·A+B·SP, 그리고 **새로 누른 방향**.
  방향을 뗀 것은 무시합니다 — 점프 중 방향을 놓아도 착지 기술은 살아야 하니까요.
- 코어판에도 같은 규칙을 넣었습니다.

**카드 아트 캡처가 제대로 안 되던 것**

화면이 **서는 도중에** 찍는 게 원인이었습니다. 페이드 중이거나 타일이 반만 올라온 프레임은
액자 판별을 통과해도 그림이 반쪽입니다.

- **280ms 간격으로 두 번 떠서 완전히 같을 때만** 담습니다(화면이 멈춰 있다는 뜻).
- 실패하면 0.5초 뒤 다시 노립니다 — 카드 화면에 머무는 동안 계속.
- 담으면 **"🎴 카드 아트 담았다 — n/4"** 로 알려 줍니다.

회귀 103건 통과.

## v0.5.6 — 홈 화면 설치(PWA) · 성능 측정 · 귀신터치와 뜬금 비오의 잡기

**고친 것 (사용자 제보 둘)**

- **판이 끝나면 뜬금없이 "온다! 비오의!"** — 승부가 난 뒤의 **승리 포즈도 액션ID가 0x180을 넘습니다.**
  그걸 필살기 발동으로 읽고 있었습니다. 이제 **둘 다 살아 있고 KO 처리 전일 때만** 기술로 봅니다.
  라운드가 새로 서면(양쪽 만피) KO 표식을 풀어, 다음 라운드 기술 알림이 막히지 않습니다.
- **SP가 귀신터치마냥 눌리던 것** — SP를 누르면 바로 못 낼 때 최대 1.5초까지 **예약**해 뒀다가
  낼 수 있게 되면 냅니다(선입력 게이트·공중 재시도·착지 대기). 라운드가 끝나는 순간 누른 입력이
  그대로 남아 **다음 라운드에서 뒤늦게 터졌습니다.** 이제 전투가 끝나면 예약을 통째로 버립니다.
  두 고침 모두 코어판에도 같이 넣었습니다.

**PWA — 홈 화면에 설치**

- 상단에 **[설치]** 버튼이 뜹니다(설치 가능한 브라우저에서만). 누르면 홈 화면에 들어갑니다.
- 설치하면 **브라우저 껍데기 없이 전체화면**으로 뜨고, 주소창이 먹던 세로 공간을 회수합니다.
- **오프라인으로 돕니다.** 에뮬 자산(약 5MB)을 서비스워커가 캐시합니다.
  롬은 캐시에 넣지 않습니다 — 롬은 예전처럼 IndexedDB 에만 있습니다.
- 새 판이 올라오면 알려 주고, 새로고침하면 바뀝니다.

**성능 측정 — 브라우저끼리 숫자로 비교**

- 설정 맨 아래 **[측정 시작]**. 16초 동안 두 구간을 잽니다 — ① 무입력 8초 ② 디패드 문지르기 8초.
- 재는 값: **에뮬 fps**(화면 표시가 아니라 게임이 실제로 돈 프레임), rAF 간격 중앙값/p95/최대,
  33ms 초과 끊김 횟수. 기기·브라우저·설정도 같이 찍힙니다.
- **결과 복사** 버튼이 있습니다. 삼성 브라우저와 크롬에서 각각 돌려 붙여 넣으면 바로 비교됩니다.

회귀 101건 통과.

## v0.5.5 — 카드 그림을 정확히 잘라 세운다 · 이름판은 게임 글꼴 그대로

카드 아트가 이 게임 최고의 캐릭터 아트라는 지적을 반영했습니다. 화면째 뜨던 것을
**액자만 정확히** 잘라내고, 글자는 우리가 그리지 않습니다 — 게임이 세로로 그려 둔 이름판을 그대로 떠 옵니다.

- **카드 그림 96×96 정위치 크롭.** 코어 프론트엔드로 실기 화면(scr 6 「능력 추가」)을 떠서
  픽셀 단위로 잰 좌표를 씁니다 — 그림 `x32,y16` 96×96, 이름판 `x8,y8` 16×88, 유파판 `x136,y8`.
- **이름판·유파판은 게임 글꼴 그대로.** 왼쪽 판에는 캐릭터 이름, 오른쪽 판에는 유파가
  그림 아래에 섭니다. 우리가 그린 글씨는 한 자도 없습니다.
- **아무 화면이나 뜨지 않습니다.** 액자 위아래 두 줄이 새까만지 확인해서, 카드 그림이
  실제로 떠 있을 때만 담습니다.
- **후보 4장까지 모읍니다.** 새 카드를 볼 때마다 쌓이고, 상단 🎴 버튼이
  `카드 아트 1 → 2 → … → 스테이지만 → 끔` 순으로 돕니다. 롬별로 저장됩니다.
- 캐릭터 선택창에서 뜨던 초상 캡처는 뺐습니다 — 카드 그림이 나오면 그쪽이 항상 낫습니다.
- 회귀 97건 통과.

## v1.3 — [콤보] 버튼 폐기

약베기로 열고 상대 HP가 줄면 마무리 기술로 캔슬하는 버튼이었습니다. 없앴습니다.

25ms마다 P2 체력을 폴링해야 했고, 힙을 못 잡으면 히트컨펌 감시를 통째로 건너뛰었습니다.
가드당하거나 빗나가면 350ms 뒤 그냥 멈추는데 그 사이 시작타 경직은 이미 진 상태입니다.
기술은 SP 자리 7개와 SP 버튼에 다 걸려 있으니 기본기는 [A]로 직접 치는 편이 낫습니다.

같이 사라진 것: 캐릭터 시트의 [콤보] 탭, 약베기/강베기 선택, 키보드 V.
[SP 배치]는 탭이 하나뿐이라 탭 줄 없이 바로 열립니다.

## v1.2 — 기본 배치 갱신

SP+방향 기본 배치를 코어판·NGP판과 같은 표로 맞췄습니다. 대각선(↘·↙)과 공중까지
채워 131 → 157자리. 중립 자리는 항상 기술 1번이라 SP1과 방향 없이 누른 SP가 같습니다.


버전 표기: **0.5가 첫 공개 배포판**입니다. 그 이전의 v10.x / v1.x는 공개 전 내부 개발 리비전이며,
코드 주석과 인계 문서에 근거 기록으로 남아 있습니다.

## v1.0 — 정식 배포판

### 터치 중 끊김의 원인을 찾아 고쳤습니다

EmulatorJS가 WebGL 컨텍스트를 `alpha:true` · `premultipliedAlpha:true`로 만들고 있었습니다.
브라우저 합성기가 매 합성마다 캔버스를 페이지 배경과 블렌딩하는데, 터치 중에는
합성기가 이미 바빠서 이 한 겹이 에뮬레이터 루프를 굶깁니다.

`getContext`를 감싸 불투명으로 강제 — **디패드 드래그 51.8 → 59.6 fps, 끊김 65 → 3회.**
화면 필터가 무겁던 것도 상당 부분 같은 원인이었습니다("부드럽게" 49.1 → 59.5 fps).

### 그 밖에

- 전역 `addEventListener` 패치를 **부팅 2초 후 원본으로 복원**
  (영구 패치면 나중에 붙는 리스너까지 조용히 passive가 됩니다)
- 로더 실패 안내를 원인별로 분리 (연결 / CDN 차단 / 새로고침), 20초 무응답 감지 추가
- 파비콘 · 테마 컬러 · OG 태그 · 홈 화면 추가 메타 정리

## v0.6 — 외부 검수 반영

- **SP 버튼 아래 기술명 표시 제거.** `refreshSpLabel()`이 `setDirs()`에서도 불려서
  **디패드를 한 칸 움직일 때마다** RAM 읽기 + 객체 생성 + DOM 쓰기가 돌고 있었습니다
- **강 판정의 `box-shadow` 제거** → 배경·테두리 색 변경으로 대체.
  하필 버튼을 누르고 있는 동안 켜져서 모바일 GPU에 계속 합성을 시켰습니다
- ⚙설정에서 **[SP 연속기] 제거** — 실제로는 발동 후 A만 눌러도 이어집니다. 항상 자동 완주
- ⚙설정에서 **[콤보 마무리] 제거** — [캐릭터] → [콤보] 탭에 고르는 칸이 이미 있었습니다
- **접근성** — 다섯 버튼 지름 동일(0.170u, 360px 폭에서 61px), A·B를 26°/70°로 벌려
  인접 간격 0.198u → 0.225u

## v0.5 — 첫 공개 배포판

- **프레임 보정(자동 프레임스킵) 완전 삭제** — 연타 시 끊김의 직접 원인이었습니다
- 버튼 스킨 5종 · CSS `transition` 전부 제거 (버튼 애니메이션 off)
- 그라디언트·그림자를 선언 자체에서 제거 — 이제 **색 교체만** 합니다
- 기본 버튼 배치를 폰 비율에 맞춰 재조정 (360×800 ~ 430×932 실측)
- **불러오기 중 매크로 정지** — 기술이 나가는 도중 상태를 불러오면 입력 찌꺼기가 남던 문제
- 안내 문구 정리
